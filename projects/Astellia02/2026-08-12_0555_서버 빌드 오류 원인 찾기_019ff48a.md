# 서버 빌드 오류 원인 찾기

- Project: Astellia02
- Session ID: `019ff48a-338f-7ec2-a5e8-90447e259941`
- Workspace: `C:\Users\dumaclub\Documents\ChatGPT\Astellia02`
- Started: 2026-08-12T05:55:26.065Z
- Updated: 2026-08-12T05:55:39.5838672Z
- Messages exported: 4

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

# Files mentioned by the user:

## codex-clipboard-f48c20a7-1ece-4d02-97e4-b551676c8739.png: C:/Users/dumaclub/AppData/Local/Temp/codex-clipboard-f48c20a7-1ece-4d02-97e4-b551676c8739.png

## My request:
서버 빌드시 위의 오류가 발생하는데 원인은 무었인가요?

<image name=[Image #1] path="C:\Users\dumaclub\AppData\Local\Temp\codex-clipboard-f48c20a7-1ece-4d02-97e4-b551676c8739.png">

</image>

### Assistant

이미지의 오류 메시지와 서버 프로젝트의 참조 설정을 함께 확인해 원인을 좁혀보겠습니다. 우선 `K2Client`가 어디에서 사용되는지와, 누락됐다고 표시된 DLL이 실제 빌드 출력 경로에 생성되는지 살펴보겠습니다.

### Assistant

현재 확인된 중요한 단서는 `NFT_SERVER` 구성에서 `SharedLib`와 `Packet`이 별도 프로젝트로 먼저 빌드되어야 한다는 점입니다. 이제 실제 DLL 생성 여부와 `EgonServer`의 참조 방식·빌드 순서를 대조하고 있습니다.

### Assistant

원인은 `SharedLib`에 클라이언트 전용 네임스페이스가 조건 없이 추가된 것입니다.

[DataManager.cs](/D:/ProjectA/Trunk/Dev/Client/Assets/Script/SharedLib/DataManager/DataManager.cs:1)의 다음 코드가 최초 오류입니다.

```csharp
using K2Client;
```

이 파일은 [SharedLib.csproj](/D:/ProjectA/Trunk/Dev/Server/SharedLib/SharedLib.csproj:37)에서도 서버용 소스로 컴파일되지만, 서버 `SharedLib`에는 `K2Client` 참조가 없습니다. 따라서:

1. `CS0246 K2Client` 발생
2. `SharedLib.dll` 생성 실패
3. `SharedLib`을 참조하는 [Packet.csproj](/D:/ProjectA/Trunk/Dev/Server/Packet/Packet.csproj:58)에서 `CS0006`
4. `Packet`을 참조하는 [EgonServer.csproj](/D:/ProjectA/Trunk/Dev/Server/EgonServer/EgonServer.csproj:126)에서도 `CS0006`

즉, 화면의 `CS0006` 세 건은 모두 첫 번째 `K2Client` 오류에 따른 연쇄 오류입니다.

해결은 클라이언트 빌드에서만 해당 `using`이 적용되도록 만드는 것입니다.

```csharp
#if UNITY_CLIENT
using K2Client;
#endif
```

또는 `using K2Client;`를 삭제하고, 클라이언트 전용 코드에서 `K2Client.LoginServerConnector`처럼 전체 이름을 사용하면 됩니다. 서버 프로젝트에 클라이언트 어셈블리를 참조시키는 방식은 의존성 방향상 권장하지 않습니다.

동일한 `NFT_SERVER|x64` 빌드로 재현하여 최초 오류가 `DataManager.cs(1,7)`임을 확인했습니다. 별도로 `WMIC` 명령을 찾지 못한다는 메시지도 나오지만, 이는 현재 네 건의 직접적인 원인은 아닙니다.
