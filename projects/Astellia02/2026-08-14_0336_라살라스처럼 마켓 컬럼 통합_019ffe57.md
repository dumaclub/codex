# 라살라스처럼 마켓 컬럼 통합

- Project: Astellia02
- Session ID: `019ffe57-7250-70e3-b3cc-78b27ea257b1`
- Workspace: `C:\Users\dumaclub\Documents\ChatGPT\Astellia02`
- Started: 2026-08-14T03:36:11.925Z
- Updated: 2026-08-14T03:36:15.2100454Z
- Messages exported: 8

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

아스텔은 마켓별로 아래 컬럼으로 분리되어 있는데
Android  IOS  HiveStore  Onestore

라살라스는 1개 컬럼으로 되어 있으니 라살라스 처럼 수정할 수 있나요?
KRPID
라살라스 프로젝트 위치는 E:\Rasalas\Trunk\qa 입니다.

### Assistant

아스텔리아 쪽의 마켓별 PID 처리 구조와 라살라스의 `KRPID` 구현을 먼저 대조한 뒤, 동일한 방식으로 통합하고 영향 범위를 확인하겠습니다. 두 프로젝트의 작업 지침과 관련 참조를 함께 살펴보겠습니다.

### Assistant

검색 범위가 큰 프로젝트라 관련 파일을 좁히는 중입니다. 우선 `KRPID`, `HiveStore`, `Onestore`가 실제로 정의·소비되는 위치를 찾아 데이터 모델과 로딩 경로를 비교하겠습니다.

### Assistant

관련 문자열이 소스 코드보다 원본 테이블 파일(Excel)에 들어 있는 것으로 보입니다. 스프레드시트 스킬의 검증 절차를 사용해 양쪽 `BMStore` 계열 원본의 시트·헤더·데이터 차이를 정확히 비교한 뒤 수정하겠습니다.

### Assistant

확인 결과 단순 헤더 변경만으로는 끝나지 않습니다. 아스텔리아의 `BMStoreData` 클래스와 로더가 현재 네 컬럼을 각각 사용하고 있고, 라살라스는 `KRPID` 하나로 사전을 구성합니다. 이제 두 원본 테이블의 실제 값이 행별로 동일한지(통합 시 데이터 손실이 없는지)까지 검사한 뒤, 테이블·데이터 모델·로더를 함께 맞추겠습니다.

### Assistant

원본 파일이 약 1–2MB이지만 서식과 시트 구조가 복잡해 파싱 시간이 걸리고 있습니다. 현재 파일을 변경하지 않은 상태로 두 프로젝트의 시트 헤더와 대표 행을 읽는 중이며, 완료되는 즉시 값 충돌 여부를 기준으로 통합 방식을 확정하겠습니다.

### Assistant

중요한 차이가 확인됐습니다. 아스텔리아의 과금 상품 694개 중 540개는 Android/iOS/원스토어 PID가 실제로 서로 다릅니다(예: `.google.launchpack`, `.apple.launchpack`, `.one.launchpack`). 따라서 컬럼 이름만 `KRPID`로 합치면 기존 영수증 매칭이 깨집니다. 플랫폼 표기만 다른 동일 상품인지와 최근 데이터의 통합 규칙을 추가로 분석해, 기존 상품까지 안전하게 이관할 수 있는지 판단하고 있습니다.

### Assistant

가능하지만, 현재 상태에서 바로 통합하면 결제가 깨질 수 있어 수정은 보류했습니다.

아스텔리아 `BMStoreData` 분석 결과:

- 과금 상품 694개
- 152개는 네 마켓 PID가 동일
- 540개는 실제 PID가 서로 다름
- 12개는 플랫폼 접두사 외에도 철자·상품명이 다름

예: `com.sesisoft.astellia.google.launchpack`, `.apple.launchpack`, `.one.launchpack` :codex-file-citation{path="D:/ProjectA/Trunk/Dev/Table/BMStoreData.xlsx" purpose="source" artifact_kind="workbook" sheet="BMStoreData" range="A1:Y1223"}

코드 역시 네 PID를 각각 조회합니다: [LoaderBMStoreData.cs](D:/ProjectA/Trunk/Dev/Client/Assets/Script/SharedLib/DataManager/Loader/LoaderBMStoreData.cs:195), [ContentsManager_BMStore.cs](D:/ProjectA/Trunk/Dev/Client/Assets/Script/System/ContentsSystem/Handler/ContentsManager_BMStore.cs:109).

기존 540개 상품도 각 마켓 등록 PID를 동일하게 바꿀 예정인가요? 그렇다면 `KRPID`를 기존 Android PID로 할지, 별도의 신규 공통 PID 목록을 사용할지 알려주세요. 기준이 확정되면 테이블·데이터 클래스·로더·결제 코드를 라살라스 방식으로 함께 수정하겠습니다.
