# AOS 구글 빌드 접속 오류 확인

- Project: Astellia02
- Session ID: `019ff8ec-cd83-77b3-a376-a3618b493e60`
- Workspace: `C:\Users\dumaclub\Documents\ChatGPT\Astellia02`
- Started: 2026-08-13T02:21:36.767Z
- Updated: 2026-08-13T02:23:58.9900922Z
- Messages exported: 7

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

# Files mentioned by the user:

## codex-clipboard-ac982071-d4ab-493f-9f59-8f1b798d51da.png: C:/Users/dumaclub/AppData/Local/Temp/codex-clipboard-ac982071-d4ab-493f-9f59-8f1b798d51da.png

## My request:
구글 빌드 접속 시 해당 에러가 노출 되고 있습니다. 확인 부탁드립니다. QA 팀에서 AOS 빌드에서 위의 오류가 발생한다고 하는데 원인 확인할 수 있을까요? 원스토어 빌드나 IOS 빌드는 정상적으로 접속 되고 있습니다. 클라에서 접속 시 최조에 접속하는 웹서버에서 접속이 되지 않는데 해당기기에서 [https://controllweb.astelliam.com/](https://controllweb.astelliam.com/)  사이트 연결시 정상적으로는 접속 되었습니다.

<image name=[Image #1] path="C:\Users\dumaclub\AppData\Local\Temp\codex-clipboard-ac982071-d4ab-493f-9f59-8f1b798d51da.png">

</image>

### Assistant

오류 문구가 `404`이면서 `netErr:true`라서, 단순 인터넷 불통보다는 Google AOS 빌드가 최초 로드 URL을 다르게 만들거나 서버에서 해당 경로만 찾지 못하는 상황이 먼저 의심됩니다. 클라이언트의 URL 조합·빌드 채널 분기와 서버 라우팅/로그 지점을 함께 추적해 원인을 좁혀보겠습니다.

### Assistant

화면의 실제 판정값은 `404:False, netErr:True`입니다. 즉 서버가 404를 반환한 것이 아니라 Unity가 HTTP 응답을 받기 전 연결 단계에서 실패한 것으로 분류했습니다. 다만 브라우저 확인은 `GET /`이고 클라이언트는 같은 주소에 JSON `POST`를 보내므로, 브라우저 접속 성공만으로 앱 요청 경로·TLS·POST 처리가 정상이라고 단정할 수는 없습니다. 이제 Google/원스토어 빌드 차이를 만드는 전처리 심볼과 Android 설정을 확인하겠습니다.

### Assistant

서버 쪽에서 매우 강한 단서가 확인됐습니다. 현재 인증서는 불과 하루 전인 2026-08-12 18:22(KST)에 새로 발급된 `SSL.com TLS RSA Root CA 2022` 계열이고, 서버는 TLS 연결 중 재협상도 두 차례 요청합니다. 장애 발생 시점이 인증서 교체 직후라면 우선순위가 높은 원인입니다. 특히 Chrome은 자체/갱신된 신뢰 저장소를 사용해 열리지만 Unity 앱은 Android OS/네이티브 TLS 체인을 사용해 실패할 수 있어 “기기 브라우저 정상, 앱 실패”와 양립합니다. 인증서 체인의 Android 호환성과 빌드별 런타임 차이를 더 대조하겠습니다.

### Assistant

확인 결과, 현재 가장 유력한 원인은 **관제 웹서버의 8월 12일 인증서 변경과 Google AOS 빌드의 TLS 인증서 검증 조합**입니다. 클라이언트의 URL 조합이나 HTTP 404 문제일 가능성은 낮습니다.

### 확인된 내용

- 화면에는 `404:False, netErr:True`라고 표시됩니다.
  - 실제 404 응답이 아니라 `UnityWebRequest.Result.ConnectionError`입니다.
  - DNS, TCP, TLS 인증서 검증 등 HTTP 응답 이전 단계에서 실패한 경우입니다.
- 클라이언트는 브라우저처럼 `GET /`을 호출하지 않고, JSON 본문으로 `POST /`을 호출합니다.
  - 호출 위치: [LoginManager_Download.cs](D:/ProjectA/Trunk/Dev/Client/Assets/Script/Login/LoginManager_Download.cs:96)
  - 연결 오류 판정: [LoginManager_Download.cs](D:/ProjectA/Trunk/Dev/Client/Assets/Script/Login/LoginManager_Download.cs:122)
- 현재 서버에 동일한 `Live / AOS / Ver 475` 요청을 보내면 정상적으로 HTTP 200과 런치 정보가 반환됩니다. 따라서 현재 API 라우팅이나 버전 데이터 자체는 정상입니다.
- Google/원스토어 모두 최초 관제 서버 요청 URL과 요청 코드는 같습니다. `ONE_STORE` 분기는 관제 서버 응답 성공 후 마켓 업데이트 URL에만 적용됩니다([LoginManager_Download.cs](D:/ProjectA/Trunk/Dev/Client/Assets/Script/Login/LoginManager_Download.cs:256)).
- 두 Android Manifest 모두 `INTERNET` 권한이 있으며 별도 `networkSecurityConfig`는 없습니다. 따라서 “Google 빌드만 인터넷 권한 누락” 문제도 아닙니다.

### 가장 유력한 원인

현재 `controllweb.astelliam.com`에 배포된 인증서는 다음과 같습니다.

- 발급 시작: 2026-08-12 18:22 KST
- 인증서: `*.astelliam.com`
- 체인: `SSL.com TLS Issuing RSA CA R1` → `SSL.com TLS RSA Root CA 2022`
- SSL Labs 결과: 전체 등급 A지만, 서버가 self-signed Root 인증서까지 전송하는 체인 문제(`issues=16`)가 확인됩니다. 이 값은 “self-signed root 포함”을 의미합니다. [SSL Labs API 설명](https://github.com/ssllabs/ssllabs-scan/blob/master/ssllabs-api-docs-v3.md)

해당 Root CA는 비교적 새로운 인증서로 Android AOSP CA 목록에도 2024년에 추가됐습니다. 오래된 Android OS 또는 Unity TLS 저장소에는 없을 가능성이 있습니다. [Android CA 추가 이력](https://android.googlesource.com/platform/system/ca-certificates/+/5c8317b41c95af1c70f4401cf8fc26c67c5690d2), [SSL.com CA 저장소](https://www.ssl.com/repository/)

Android 앱은 기본적으로 단말의 시스템 CA를 신뢰합니다. 반면 Chrome은 자체 Chrome Root Store를 사용할 수 있으므로 **같은 단말에서 브라우저는 정상인데 Unity 앱만 인증서 검증에 실패하는 상황이 가능합니다.** [Android Network Security 설정](https://developer.android.com/privacy-and-security/security-config), [Chrome Root Store 정책](https://www.chromium.org/Home/chromium-security/root-ca-policy/)

### 권장 대응

1. 서버팀에서 우선 인증서 변경 시점을 장애 최초 발생 시점과 대조합니다.
2. `controllweb.astelliam.com` 인증서를 다음 중 하나로 조정해 재검증합니다.
   - 기존 호환성이 확인된 인증서로 임시 복구
   - SSL.com에서 제공하는 구형 단말 호환 체인/cross-signed 체인 적용
   - 서버가 self-signed Root까지 내려보내지 않도록 `leaf + 올바른 intermediate` 체인으로 설정
3. QA에서 Google/원스토어를 반드시 같은 단말·같은 네트워크·같은 시각에 비교합니다.
   - Android 버전
   - 보안 패치 및 Google Play 시스템 업데이트 버전
   - APK/AAB 및 Unity 빌드 버전
4. 현재 클라이언트 오류 팝업은 핵심 오류 문자열을 버리고 있습니다. 아래 정보를 로그에 남기면 한 번의 재현으로 확정할 수 있습니다.
   - `request.result`
   - `request.responseCode`
   - `request.error`
   - `request.url`

동일 단말에서 최신 원스토어 빌드는 계속 성공하고 Google 빌드만 실패한다면 인증서 문제만으로는 설명이 부족하며, 그때는 Play 배포 AAB와 로컬 Google APK의 네이티브 런타임 차이를 비교해야 합니다. 다만 현재 증거상 1순위는 **8월 12일 신규 인증서 체인과 Google AOS UnityTLS 검증 간 호환 문제**입니다. 코드 수정은 하지 않았습니다.

### User

특정 기기에서만 발생하고 나머지 기기에서는 정상적으로 받고 있습니다. 특정 핸드폰에서만 그렇게 발생할 수도 있을까요?

### Assistant

네, 특정 기기에서만 발생할 수 있습니다. 특히 현재처럼 `netErr:true`이고 신규 인증서 적용 직후라면 기기별 차이가 충분히 가능합니다.

주요 원인은 다음과 같습니다.

- Android OS·보안 패치 버전에 따른 신뢰 CA 차이
- Google Play 시스템 업데이트 상태
- 기기 날짜/시간 오류
- 제조사별 TLS 구현 차이
- VPN, 광고 차단, 백신, Private DNS 등의 HTTPS 간섭
- Wi-Fi와 모바일 데이터의 DNS/IPv6 처리 차이

브라우저가 정상이어도 앱은 실패할 수 있습니다. Chrome과 Unity 앱이 동일한 인증서 저장소 및 TLS 처리 경로를 사용한다고 보장되지 않기 때문입니다.

다만 가장 중요한 확인 포인트가 있습니다.

- 같은 기기에서 Google 빌드만 실패하고 원스토어 빌드는 성공한다면
  → 단순한 기기 인증서 문제만으로는 부족합니다. Google AAB/APK의 Unity 런타임, 네이티브 라이브러리 또는 실제 배포 바이너리 차이를 봐야 합니다.

- 실패 기기에서는 Google·원스토어 모두 실패하고, 다른 기기에서는 모두 성공한다면
  → 해당 기기의 인증서 저장소, OS 업데이트, 네트워크 환경 문제가 매우 유력합니다.

가장 빠른 확인 순서는 다음과 같습니다.

1. 실패 기기의 Android 버전과 보안 패치 날짜 확인
2. 자동 날짜/시간 활성화 확인
3. VPN·Private DNS·광고 차단·백신 비활성화 후 재시도
4. Wi-Fi와 모바일 데이터 각각 테스트
5. 같은 기기에 Google 빌드와 원스토어 빌드를 번갈아 설치해 테스트
6. 클라이언트 로그에 `request.error`를 출력

현재 코드는 실제 TLS 오류 문구를 버리고 `netErr:true`만 표시하고 있습니다. 아래 값이 확보되면 거의 바로 확정할 수 있습니다.

```csharp
Debug.LogError(
    $"Launch request failed. " +
    $"result={request.result}, " +
    $"code={request.responseCode}, " +
    $"error={request.error}, " +
    $"url={request.url}");
```

예를 들어 다음처럼 나오면 원인을 구분할 수 있습니다.

- `Curl error 60`, `Cert verify failed` → 인증서 체인/기기 CA 문제
- `Cannot resolve destination host` → DNS 문제
- `Cannot connect to destination host` → 네트워크·방화벽·IPv6 문제
- `SSL connect error` → TLS 버전/암호화 호환 문제

따라서 “특정 기기에서만 발생”은 충분히 가능한 현상이며, 현재는 **해당 기기의 인증서·TLS 환경을 1순위로 확인하되, 같은 기기에서 원스토어 빌드가 정말 성공하는지 교차 테스트하는 것이 핵심**입니다.
