# 일일의뢰 갱신 오류 확인

- Project: Astellia02
- Session ID: `01a01272-8796-7242-828e-fd28d5766a7b`
- Workspace: `C:\Users\dumaclub\Documents\ChatGPT\Astellia02`
- Started: 2026-08-18T01:18:11.389Z
- Updated: 2026-08-18T01:19:19.2683029Z
- Messages exported: 9

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

# Files mentioned by the user:

## codex-clipboard-fc03c58e-7d89-4eeb-afbb-43828ba77929.png: C:/Users/dumaclub/AppData/Local/Temp/codex-clipboard-fc03c58e-7d89-4eeb-afbb-43828ba77929.png

## My request:
일일의뢰가 갱신되지 않았다고 하는데 어떤 문제인지 확인해 주세요.
일일의뢰는 다음날 오전 10시에 새로운 내용으로 갱신되어야 하는데 기존 의뢰로 남아 있다고 합니다.
아래 패킷을 사용하는 컨텐츠 입니다. 코드가 맞게 되어있는지 오류가 발생할 수 있는 부분이 있는지 확인해 주세요.
        CS_DailyQuestInfo = 596,
        SC_DailyQuestInfo = 597,
        CS_DailyQuestAction = 598,
        SC_DailyQuestAction = 599,

<image name=[Image #1] path="C:\Users\dumaclub\AppData\Local\Temp\codex-clipboard-fc03c58e-7d89-4eeb-afbb-43828ba77929.png">

</image>

### Assistant

패킷 596~599의 서버·클라이언트 처리 경로와 오전 10시 갱신 기준 계산을 함께 추적하겠습니다. 먼저 저장소 지침과 관련 심볼을 찾은 뒤, “정보 조회 시 갱신”과 “접속 유지 중 자동 갱신”이 모두 정상인지 확인하겠습니다.

### Assistant

관련 처리는 `GameNode`의 `ActorUserModel_Quest.cs`에 집중되어 있습니다. 현재는 10시가 지난 뒤 서버가 실제로 의뢰 목록을 재선정하는지, 재선정 결과가 저장·전송되는지, 접속 중인 캐릭터에게 갱신 패킷이 전달되는지를 분리해서 보고 있습니다.

### Assistant

현재까지 확인된 핵심은 두 가지입니다. 서버의 실제 목록 재선정 함수는 10시 기준을 사용하지만, 접속 유지 중 주기적으로 도는 초기화 경로는 같은 상수를 부호 반대로 넘겨 자정 기준으로 계산되고 있습니다. 또한 10시 정각에 596 응답을 자동 푸시하는 구조가 아니라, 클라이언트가 다시 596을 요청해야만 목록 재선정과 동기화가 실행됩니다. 이 두 경로가 증상을 만들 수 있는지 호출 조건과 데이터 보존 규칙까지 더 검증하겠습니다.

### Assistant

확인 결과, 패킷 번호/등록 자체는 정상입니다. 문제는 **오전 10시 갱신 트리거와 시간 계산, 실패 처리 방식**에 있습니다. 화면에 `완료` 상태가 그대로 남은 점을 보면 실제 갱신 함수가 실행되지 않았거나 중간 실패했을 가능성이 높습니다.

### 주요 원인

1. **접속 유지 중 오전 10시에 자동 갱신되지 않습니다.**

서버에서 목록을 재선정하는 `CheckAllResetDailyQuestProc()`는 다음 경우에만 실행됩니다.

- 클라이언트가 `CS_DailyQuestInfo(596)` 요청
- 존 입장

[ActorUserModel_Quest.cs:868](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:868>)
[ActorUserModel.cs:919](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel.cs:919>)

클라이언트도 창을 열거나 수락 시 날짜가 지났다고 판단한 경우에만 596을 요청하며, 10시 경계 감시나 주기 갱신은 없습니다.

[UIDailyQuestWindow.cs:238](<D:/ProjectA/Trunk/Dev/Client/Assets/Script/UI/DailyQuest/UIDailyQuestWindow.cs:238>)
[UIDailyQuestWindow.cs:688](<D:/ProjectA/Trunk/Dev/Client/Assets/Script/UI/DailyQuest/UIDailyQuestWindow.cs:688>)

따라서 **10시 이전부터 의뢰 창을 열어 둔 경우 기존 내용이 계속 표시되는 현상**이 발생합니다.

2. **초기화 시간 계산의 부호가 서로 다릅니다.**

`K2Const.ADD_INIT_HOUR` 값은 `-10`입니다.

[K2Const.cs:100](<D:/ProjectA/Trunk/Dev/Client/Assets/Script/SharedLib/K2Const.cs:100>)

그런데 접속 중 주기적으로 호출되는 초기화 경로에서는 이 값을 그대로 넘깁니다.

[ActorUserModel_Quest.cs:1690](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:1690>)

`GetLastDailyResetTime()`은 음수를 받으면 `0`으로 보정하므로 이 경로는 **오전 10시가 아니라 자정 기준**이 됩니다.

[K2Common.cs:506](<D:/ProjectA/Trunk/Dev/Client/Assets/Script/SharedLib/K2Common.cs:506>)

반면 실제 일일의뢰 목록 검사에서는 `* -1`, 즉 `10`을 넘겨 올바르게 계산합니다.

[ActorUserModel_Quest.cs:2404](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2404>)

3. **갱신 실패가 클라이언트에 전달되지 않습니다.**

`ResetDailyQuest()` 반환값을 확인하지 않고, `SC_DailyQuestInfo(597)`는 항상 성공으로 보냅니다.

[ActorUserModel_Quest.cs:2390](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2390>)
[ActorUserModel_Quest.cs:870](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:870>)

테이블 누락, 슬롯 데이터 오류 등이 발생하면 서버는 기존 목록을 보내면서도 `Success`를 반환할 수 있습니다.

또한 `checkDate`를 갱신 초반에 먼저 현재 시간으로 변경합니다.

[ActorUserModel_Quest.cs:2440](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2440>)

이후 로직이 실패하면 목록은 그대로인데 날짜만 갱신되어 **당일에는 다시 초기화를 시도하지 않는 상태**가 될 수 있습니다.

4. **일부 상태는 의도적으로 다음날 유지됩니다.**

`Proceeding`과 `RewardAndComplete` 상태는 다음날에도 기존 의뢰를 유지하도록 구현되어 있습니다.

[ActorUserModel_Quest.cs:2469](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2469>)

요구사항이 “모든 의뢰를 새로운 내용으로 갱신”이라면 이 코드는 요구사항과 맞지 않습니다. 다만 화면의 `완료` 표시는 `Completed` 상태이며, 이 상태는 정상 갱신 시 교체되어야 하므로 이번 화면은 단순 이월보다 **갱신 미실행/실패** 쪽에 가깝습니다.

### 추가 발견

- `GetRefreshTime()`의 `result.AddDays(-1)` 반환값을 대입하지 않아 실제 날짜가 변경되지 않습니다. 현재는 호출처가 없어 직접 원인은 아닙니다.
  [UIDailyQuestWindow.cs:202](<D:/ProjectA/Trunk/Dev/Client/Assets/Script/UI/DailyQuest/UIDailyQuestWindow.cs:202>)
- `NeedRefresh()`는 존 정보의 null 여부를 확인하기 전에 `.checkDate`에 접근합니다.
  [UIDailyQuestWindow.cs:212](<D:/ProjectA/Trunk/Dev/Client/Assets/Script/UI/DailyQuest/UIDailyQuestWindow.cs:212>)
- 부족한 슬롯 복구 시 시작 번호가 `Count - 1`로 되어 있어 중복 슬롯/DB 엔티티가 만들어질 수 있습니다. `Count + 1`이 의도에 가까워 보입니다.
  [ActorUserModel_Quest.cs:2452](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2452>)

### 권장 수정 방향

- 초기화 시간을 `Defines.MISSION_DAILY_RESET_HOUR` 같은 양수 `10`으로 통일
- 10시 경계 감지 시 서버에서 `CheckAllResetDailyQuestProc()` 실행 후 `SC_DailyQuestInfo` 푸시
- 모든 `CS_DailyQuestAction(598)` 처리 전에도 날짜 갱신 검사
- `ResetDailyQuest()` 성공 후에만 `checkDate` 갱신
- 실패 결과를 `SC_DailyQuestInfo/Action`에 반영하고 로그 기록
- 진행 중/보상 대기 의뢰를 다음날 유지할지 기획 확인

코드는 변경하지 않았습니다. 가장 우선적으로는 **10시 자동 갱신 트리거 추가와 `ResetDailyQuest()` 실패 결과 처리**가 필요합니다.

### User

2가지 작업을 진행하였습니다. 추가로 작업 해야 할 항목을 알려주세요.
1) CheckResetDailyQuestProc 함수 로그 추가
2) ResetDailyQuest 함수에서 아래 코드를 가장 마지막으로 옮겼습니다.

if (dQIEntity.checkDate > resetTm)
    dQIEntity.SetDailyQuestInfoCount(dQIEntity.completeCount, dQIEntity.refreshCount + 1);
else
    dQIEntity.SetDailyQuestInitInit();

### Assistant

반영하신 두 변경의 실제 diff를 먼저 확인한 뒤, 아직 남아 있는 갱신 경로 문제를 우선순위별로 정리하겠습니다. 특히 `checkDate` 이동이 수동 새로고침 비용 판정까지 안전하게 유지하는지 함께 보겠습니다.

### Assistant

두 번째 변경은 “실패했는데 날짜만 갱신되는 문제”는 막지만, 현재 형태로는 수동 `새로 고침`의 횟수·비용 계산이 한 단계 늦어집니다. 예를 들어 첫 유료 갱신이 무료가 되고 최대 갱신 횟수보다 한 번 더 허용될 수 있어, 단순히 마지막으로 이동하는 방식 대신 계산값과 최종 커밋을 분리해야 합니다.

### Assistant

추가 작업이 필요합니다. 특히 2번 변경은 현재 그대로 적용하면 **수동 새로고침 비용·횟수 오류**가 생깁니다.

## 필수 수정

1. `refreshCount`는 마지막에 저장하되, 계산은 함수 초반에 해야 합니다.

현재는 기존 `refreshCount`로 최대 횟수와 비용을 검사한 뒤 마지막에 증가시킵니다.

[ActorUserModel_Quest.cs:2454](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2454>)
[ActorUserModel_Quest.cs:2510](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2510>)
[ActorUserModel_Quest.cs:2593](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2593>)

이 상태에서는:

- 첫 수동 새로고침 비용이 무료가 될 수 있음
- 비용 단계가 한 단계씩 늦게 적용됨
- 최대 횟수보다 한 번 더 갱신 가능

다음처럼 계산값을 먼저 만들어 검증하고, 실제 엔티티 반영만 마지막에 해야 합니다.

```csharp
bool isDailyReset = dQIEntity.checkDate < resetTm;
int targetRefreshCount = isDailyReset
    ? 0
    : dQIEntity.refreshCount + 1;

// 최대 횟수와 비용은 targetRefreshCount 기준으로 검사
```

성공 직전에만 다음을 호출합니다.

```csharp
if (isDailyReset)
    dQIEntity.SetDailyQuestInitInit();
else
    dQIEntity.SetDailyQuestInfoCount(
        dQIEntity.completeCount,
        targetRefreshCount);
```

2. 비용 검사 전에 의뢰를 지우는 순서를 수정해야 합니다.

현재 기존 의뢰를 먼저 `questId = 0`으로 만든 다음 비용을 확인합니다.

[ActorUserModel_Quest.cs:2478](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2478>)
[ActorUserModel_Quest.cs:2510](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2510>)

재화 부족이나 비용 테이블 오류가 발생하면 서버 내부 목록은 이미 지워진 상태가 됩니다. 다음 순서가 안전합니다.

1. 갱신 모드와 `targetRefreshCount` 계산
2. 최대 횟수 확인
3. 비용 테이블 및 보유 재화 확인
4. 신규 의뢰·보상 후보 생성 및 검증
5. 재화 차감
6. 기존 목록 교체
7. `checkDate`, `refreshCount` 최종 반영

3. 로그만 남기지 말고 실패 결과를 패킷에 전달해야 합니다.

현재 `ResetDailyQuest()`가 실패해도 `SC_DailyQuestInfo.result`는 항상 `Success`입니다.

[ActorUserModel_Quest.cs:868](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:868>)

권장 변경:

- `CheckResetDailyQuestProc()`가 `ePacketCommonResult` 반환
- `CheckAllResetDailyQuestProc()`도 실패 결과 반환
- `SC_DailyQuestInfo.result`에 해당 결과 설정
- 실패 시 기존 목록을 정상 갱신 결과처럼 보내지 않기

로그에는 `checkDate`, `resetTm`, `refreshCount`, 호출 원인도 추가하는 것이 좋습니다. `CheckResetDailyQuestProc()02` 같은 명칭은 `reason:Expired` 등으로 변경하는 편이 검색에 유리합니다.

4. 오전 10시 자동 갱신 트리거를 추가해야 합니다.

현재 변경만으로는 10시 이전부터 접속하거나 창을 열어 둔 사용자의 화면이 자동으로 바뀌지 않습니다.

또한 주기 초기화 경로는 여전히 `-10`을 전달해 자정으로 계산됩니다.

[ActorUserModel_Quest.cs:1690](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:1690>)
[K2Common.cs:506](<D:/ProjectA/Trunk/Dev/Client/Assets/Script/SharedLib/K2Common.cs:506>)

이를 다음 중 하나로 통일해야 합니다.

```csharp
Defines.MISSION_DAILY_RESET_HOUR
```

또는

```csharp
-K2Const.ADD_INIT_HOUR
```

10시 경계를 감지했을 때 다음까지 실행해야 합니다.

- `CheckAllResetDailyQuestProc()`
- 최신 목록으로 `SC_DailyQuestInfo(597)` 전송
- 열린 UI 갱신

클라이언트에서도 창이 열린 상태라면 10시 경계에 596을 한 번 요청하는 보조 방어 로직을 두는 것이 좋습니다.

## 기획 확인 필요

현재 `Proceeding`과 `RewardAndComplete` 의뢰는 다음날로 이월됩니다.

[ActorUserModel_Quest.cs:2475](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2475>)

“다음날 모든 의뢰가 새로운 내용으로 갱신”이 요구사항이면 이 이월 로직도 제거해야 합니다. 진행 중/보상 대기 의뢰의 이월이 기획이라면 유지 가능합니다.

## 함께 수정 권장

- 부족한 슬롯 생성 시작값 `Count - 1`을 안전한 슬롯 탐색 또는 `Count + 1`로 수정
  [ActorUserModel_Quest.cs:2458](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2458>)
- 보상 수령 결과를 `result`에 대입하지 않는 오류 수정
  [ActorUserModel_Quest.cs:2157](<D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2157>)
- 클라이언트 `NeedRefresh()`의 null 접근 및 서버와 비교 연산자 통일
  [UIDailyQuestWindow.cs:212](<D:/ProjectA/Trunk/Dev/Client/Assets/Script/UI/DailyQuest/UIDailyQuestWindow.cs:212>)
- `result.AddDays(-1)` 반환값 미대입 수정
  [UIDailyQuestWindow.cs:202](<D:/ProjectA/Trunk/Dev/Client/Assets/Script/UI/DailyQuest/UIDailyQuestWindow.cs:202>)

우선순위는 **① 카운트/비용 계산 회귀 수정 → ② 실패 결과 패킷 전달 → ③ 오전 10시 자동 트리거 → ④ 이월 정책 확인** 순서가 적절합니다. 파일은 변경하지 않았습니다.
