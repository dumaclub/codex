# questId 0 입력 원인 확인

- Project: Astellia02
- Session ID: `01a041b5-67ab-7190-8354-af8d9d7866a4`
- Workspace: `C:\Users\dumaclub\Documents\ChatGPT\Astellia02`
- Started: 2026-08-27T05:33:23.059Z
- Updated: 2026-08-27T05:33:29.5777599Z
- Messages exported: 4

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

이함수가 먼가 잘못 된것 같습니다. questId 값이 무조건 0으로 들어옵니다. 어떤 의도가 있는지 아니면 그냥 오류인지 확인해 주세요.&#x20;
public virtual void Initialize(long uId, long charId, int zoneId, int questNum, int questId)

### Assistant

해당 `Initialize`의 선언부뿐 아니라 호출부, 오버라이드, 내부 필드 대입까지 함께 추적해 보겠습니다. `questNum`과 `questId`의 역할이 뒤바뀌었는지, 호출자가 의도적으로 0을 넘기는지도 확인하겠습니다.

### Assistant

대상은 일반 퀘스트가 아니라 `DailyQuestEntity`의 초기화 함수로 확인됐습니다. 이제 생성 경로와 DB 로드/신규 생성 경로를 나눠서 보면, `questId = 0`이 “빈 일일퀘스트 슬롯”을 뜻하는 설계인지 실제 인자 전달 오류인지 판별할 수 있습니다.

### Assistant

확인 결과, `questId = 0`은 호출 실수가 아니라 **빈 일일 퀘스트 슬롯을 생성하기 위한 의도적인 값**입니다.

근거는 다음과 같습니다.

- `DailyQuestEntity.Initialize()` 호출부는 두 곳뿐이며 모두 마지막 인자로 명시적으로 `0`을 전달합니다.
  - [ActorUserModel_Quest.cs:2489](D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2489)
  - [ActorUserModel_Quest.cs:2530](D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2530)

- 먼저 `questNum`으로 고정 슬롯을 만들고 `questId = 0`으로 비워 둔 뒤, 랜덤 퀘스트 선정 후 `changeDailyQuest()`에서 실제 퀘스트 ID를 넣습니다.
  - [ActorUserModel_Quest.cs:2617](D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2617)
  - [QuestEntity.cs:349](D:/ProjectA/Trunk/Dev/Server/EgonServer/Database/Entities/QuestEntity.cs:349)

- `QuestModel`에서도 `questId == 0`인 엔티티를 빈 슬롯으로 취급하고 패킷 전송에서 제외합니다.
  - [QuestModel.cs:185](D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Quest/QuestModel.cs:185)
  - [QuestModel.cs:2206](D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Quest/QuestModel.cs:2206)

다만 `Initialize()` 구현에는 오류성 코드가 있습니다. `questId = 0`이 정상 입력인데도 `GetQuestData(0)`을 호출하고 “dailyquest data를 찾지 못했다”는 에러 로그를 남깁니다. 같은 데이터를 두 번 조회하며 첫 번째 `dailyQuestData` 변수는 이후 사용되지도 않습니다.

- [QuestEntity.cs:319](D:/ProjectA/Trunk/Dev/Server/EgonServer/Database/Entities/QuestEntity.cs:319)
- [QuestEntity.cs:325](D:/ProjectA/Trunk/Dev/Server/EgonServer/Database/Entities/QuestEntity.cs:325)

따라서 결론은 다음과 같습니다.

- `questId`가 0으로 전달되는 것: 의도된 동작
- `Initialize()`에서 0을 실제 퀘스트 ID처럼 조회하고 에러를 기록하는 것: 구현 오류
- 함수 이름과 매개변수 구조도 의도를 잘 드러내지 못함

추가로 [ActorUserModel_Quest.cs:2484](D:/ProjectA/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Quest.cs:2484)의 `startNum = dqEntityDic.Count - 1`도 매우 의심스럽습니다. 기존 슬롯과 중복되는 번호부터 생성하므로 일반적으로는 `Count + 1`이어야 합니다. 중복 슬롯과 `questId = 0` DB 데이터가 남는 현상이 있다면 이 부분이 실제 원인일 가능성이 큽니다. 이번에는 요청대로 분석만 했으며 코드는 변경하지 않았습니다.
