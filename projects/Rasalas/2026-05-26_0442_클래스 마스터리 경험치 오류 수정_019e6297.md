# 클래스 마스터리 경험치 오류 수정

- Project: Rasalas
- Session ID: `019e6297-4700-7d70-a0ed-17faea6d0ad8`
- Workspace: `E:\Rasalas\Trunk\qa`
- Started: 2026-05-26T04:42:21.825Z
- Updated: 2026-05-26T04:42:28.0588537Z
- Messages exported: 6

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

<environment_context>
  <cwd>E:\Rasalas\Trunk\qa</cwd>
  <shell>powershell</shell>
  <current_date>2026-05-26</current_date>
  <timezone>Asia/Seoul</timezone>
</environment_context>

### User

클래스 마스터리 경험치 아이템 오류 수정

1.상급 숙련도 물약 1레벨 -> 만렙까지 772개 물약 필요
2.1000개 한번에 요청하면 만렙 전까지는 나머지 물약 버림 처리됨
  ex>(1개만 써도 만렙되는 상황인데 100개쓰면 99개 날라감)
3.나머지는 숙련도 경험치(숙련도 코인으로 변환) 되어야함..

위의 이슈로 아래처럼 코드 수정하였는데 맞게 된 것인지 확인해 주세요. 

수정전 
            // 포인트 증가는 9레벨 이후부터 가능 
            if (_classMasteryExpDic[classType].masteryLv < K2Const.CLASS_MASTERY_BASE_SLOT_COUNT) { return 0; }

수정후
            // 포인트 증가는 9레벨 이후부터 가능 
//            if (_classMasteryExpDic[classType].masteryLv < K2Const.CLASS_MASTERY_BASE_SLOT_COUNT) { return 0; }
            if(addCmPoint <= 0) { return 0; }

### Assistant

관련 로직을 직접 따라가 보겠습니다. 변경 한 줄만 보면 의도는 이해되지만, 실제로 `addCmPoint`가 “초과 사용분”을 정확히 담고 있는지와 9레벨 이전 반환 조건이 다른 보상 흐름을 막고 있었는지가 핵심이라 주변 코드를 같이 확인할게요.

### Assistant

`ClassMasteryModel.cs`에 해당 변경이 있고, git 루트는 현재 폴더가 아닌 것 같습니다. 우선 서버 쪽 실제 계산 흐름과 엔티티의 `AddMasteryExp`/`SubMasteryPoint` 동작을 좁혀서 읽어볼게요.

### Assistant

흐름상 아이템 사용은 `consumptionCount * ItemFunctionList[0]`를 한 번에 경험치로 넣고, 그 뒤에 레벨업을 몰아서 체크합니다. 그래서 기존 `masteryLv < 9` 검사는 “이번 요청으로 9레벨을 넘는 경우”에도 아직 레벨업 처리 전이라 코인 반환을 막는 구조였네요.

### Assistant

**검토 결과**

수정 방향은 맞습니다.  
기존 조건은 [ClassMasteryModel.cs](E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/Section/ClassMasteryModel.cs:255)에서 `masteryLv < 9`이면 바로 `return 0`을 해버리는데, 아이템 일괄 사용 흐름은 “경험치 추가 → 코인 반환 계산 → 그 다음 레벨업 체크” 순서입니다. 그래서 1레벨에서 1000개를 쓰면 실제로는 초과분 `addCmPoint`가 생겨도, 아직 `masteryLv`가 9로 갱신되기 전이라 코인 지급이 막혔습니다.

변경 후처럼 `addCmPoint <= 0`일 때만 반환하면, “이번 요청에서 만렙 초과 경험치가 발생했는가”를 기준으로 코인 변환이 진행되므로 1000개 사용 시 초과분이 `masteryPoint`로 누적되고 `MasterCoinMaxPoint` 단위만큼 숙련도 코인으로 지급됩니다.

다만 정리하면 좋을 부분이 있습니다.

1. 주석은 바꾸는 게 좋습니다.  
   현재 `// 포인트 증가는 9레벨 이후부터 가능`은 변경된 로직과 살짝 안 맞습니다.  
   예: `// 만렙 초과 경험치가 있는 경우 숙련도 포인트를 코인으로 변환`

2. 주석 처리된 기존 코드는 제거하는 편이 좋습니다.

3. 안전하게 하려면 `coinRate <= 0` 방어도 추가하면 좋습니다. `ChangeCoin()`에는 이미 비슷한 방어가 있습니다.

추가로, 이미 과거에 이 문제를 겪은 유저 보정이 필요하다면 별도 처리도 봐야 합니다. 기존 코드도 `addCmPoint` 자체는 `masteryPoint`에 더한 뒤 `return 0` 했기 때문에, DB에 포인트가 남아 있을 가능성이 있습니다. 이 수정은 앞으로의 아이템 사용 흐름은 고치지만, 과거 누적분을 자동 지급하진 않습니다.
