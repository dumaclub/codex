# BMStoreData 초기화 위치 찾기

- Project: Rasalas
- Session ID: `019e48fc-8e73-7ab1-8af0-5fef8afcf742`
- Workspace: `E:\Rasalas\Trunk\qa`
- Started: 2026-05-21T05:22:51.635Z
- Updated: 2026-05-21T05:23:13.2949977Z
- Messages exported: 6

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

BMStoreData 에 일일/주간/월간 스텝업 상품이 초기화 되는 코드가 어디인지 확인해 주세요.

### Assistant

코드 경로를 먼저 따라가 보겠습니다. `BMStoreData` 정의와 “stepup/step up” 관련 갱신 지점을 같이 찾아서, 실제 초기화가 어디서 일어나는지 흐름까지 확인할게요.

### Assistant

`BMStoreData`는 테이블/클라이언트/서버 쪽 파일이 같이 있습니다. 이제 상품 타입명이 코드에서 어떤 문자열/enum으로 쓰이는지 좁혀서 서버 초기화 로직을 따라가 보겠습니다.

### Assistant

테이블 로더에서는 `BMStepData`를 `LinkTableEventID` 기준으로 묶기만 하고, 일/주/월 초기화는 하지 않습니다. 실제 구매 제한/초기화는 서버 모델 쪽일 가능성이 높아서 `eUpdateCycle` 사용처를 추적하겠습니다.

### Assistant

핵심 지점이 보입니다. `BMModel.cs`에서 `UpdateCycle`을 기준으로 리셋 시간을 계산하고, 스텝업 구매 딕셔너리를 같이 지우는 흐름이 있어서 그 주변 코드를 자세히 확인하겠습니다.

### Assistant

확인 결과, **일일/주간/월간 BMStoreData 구매 제한 및 스텝업 진행 초기화는 서버의 `BMModel.CheckBMRefresh()`에서 처리**됩니다.

핵심 위치는 [BMModel.cs](E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/BMModel.cs:1468) 입니다.

```csharp
switch (date.UpdateCycle)
{
    case eUpdateCycle.Daily:
        updateDate = K2Common.GetLastDailyResetTime(...);
        break;
    case eUpdateCycle.Weekly:
        updateDate = K2Common.GetLastWeeklyResetTime(...);
        break;
    case eUpdateCycle.Monthly:
        updateDate = K2Common.GetLastMonthlyResetTime(...);
        break;
}

if (entity.lastRefreshDate < updateDate)
{
    entity.ResetBuyCount();
    removeStepUpPurchas(date); // 스텝업 횟수 초기화
}
```

호출 지점은 주로 다음입니다.

- [BMModel.cs:353](E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/BMModel.cs:353) `SetBMList()`에서 상품 리스트 구성 전 호출
- [BMModel.cs:860](E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/BMModel.cs:860) `CheckBmpBopBMBuy()` 구매 가능 체크 중 호출
- [BMModel.cs:905](E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/BMModel.cs:905) `CheckBMBuy()` 구매 체크 중 호출

`BMStoreData` 쪽에서는 리셋 주기 enum과 필드만 정의되어 있습니다.

- [BMStoreData.cs:110](E:/Rasalas/Trunk/qa/Client/Assets/Script/SharedLib/DataManager/DataClass/BMStoreData.cs:110) `eUpdateCycle`
- [BMStoreData.cs:225](E:/Rasalas/Trunk/qa/Client/Assets/Script/SharedLib/DataManager/DataClass/BMStoreData.cs:225) `BMStoreData.UpdateCycle`

정리하면, **BMStoreData의 `UpdateCycle` 값이 Daily/Weekly/Monthly인 상품은 `CheckBMRefresh()`에서 주기별 reset time을 계산하고, `ResetBuyCount()`와 `removeStepUpPurchas()`로 구매 횟수와 스텝업 진행 상태를 초기화**합니다.
