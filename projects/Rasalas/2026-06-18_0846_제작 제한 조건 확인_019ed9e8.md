# 제작 제한 조건 확인

- Project: Rasalas
- Session ID: `019ed9e8-eddf-71e2-a2ce-de82e554d7f6`
- Workspace: `E:\Rasalas\Trunk\qa`
- Started: 2026-06-18T08:46:21.664Z
- Updated: 2026-06-18T08:47:09.2659677Z
- Messages exported: 14

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

<environment_context>
  <cwd>E:\Rasalas\Trunk\qa</cwd>
  <shell>powershell</shell>
  <current_date>2026-06-18</current_date>
  <timezone>Asia/Seoul</timezone>
  <filesystem><workspace_roots><root>E:\Rasalas\Trunk\qa</root></workspace_roots><permission_profile type="managed"><file_system type="restricted"><entry access="read"><special>:root</special></entry><entry access="write"><path>E:\Rasalas\Trunk\qa</path></entry><entry access="write"><special>:slash_tmp</special></entry><entry access="write"><special>:tmpdir</special></entry><entry access="read"><path>E:\Rasalas\Trunk\qa\.git</path></entry><entry access="read"><path>E:\Rasalas\Trunk\qa\.agents</path></entry><entry access="read"><path>E:\Rasalas\Trunk\qa\.codex</path></entry></file_system></permission_profile></filesystem>
</environment_context>

### User

제작식에서 신규/복귀 유저가 제작하게 되는 제한 조건이 있는지 확인해 주세요. 아래 패킷입니다. 
        // crafting
        CS_ItemCraft = 401,                         // 제작 요청
        SC_ItemCraft = 402,                         // 제작 응답

### Assistant

제작 요청 패킷이 서버에서 어디로 흘러가는지부터 따라가 보겠습니다. `CS_ItemCraft` 핸들러와 제작식/조건 체크 쪽을 같이 찾아서 신규/복귀 유저 플래그가 끼어드는지 확인할게요.

### Assistant

첫 검색은 범위가 너무 넓어서 웹 리소스와 Unity 임시 파일까지 빨려 들어갔습니다. 서버/프로토콜 쪽으로 범위를 좁혀서, 실제 401번 요청을 처리하는 코드와 `ItemCraftingData`를 읽는 모델만 따라가겠습니다.

### Assistant

`Craft_NewReturn` 로컬라이즈가 있어서 UI 카테고리명은 존재합니다. 다만 질문하신 건 “제작 가능 여부 제한”이므로, 이제 JSON/CSV 덤프는 제외하고 C# 로직만 보겠습니다.

### Assistant

서버 로직에서 핵심 단서를 찾았습니다. `ActorUserModel_Inventory.ItemCraft()` 초입에 `craftingData.NewUserFlag`를 `UserEventModel.CheckNewReturnUserFlag()`로 검사하고, 실패하면 `NotAcceptNewReturnUser`로 제작을 막습니다. 이제 실제 테이블에 그 값이 들어간 제작식이 있는지도 확인하겠습니다.

### Assistant

테이블 쪽 1차 확인에서는 `CraftingData.json`에 숫자형 비-0 `NewUserFlag`는 안 잡혔습니다. 혹시 enum 문자열로 저장되는 구조일 수 있어서, 필드 전체 분포를 파싱해서 확인하겠습니다.

### Assistant

현재 클라이언트 밸런스 JSON에는 실제로 `NewUserFlag`가 들어간 제작식이 있습니다. 숫자값 기준으로 `1=신규`, `2=복귀`, `4=신규/복귀 아님`이고, 전체 개수와 제작식 ID를 뽑아보겠습니다.

### Assistant

패킷 경로도 같이 확인 중입니다. 401번은 `ThrowedHandler`에서 기본 검증 후 `ZoneItemCraftCmd`로 들어가고, 실제 제한 체크는 존 명령에서 `ActorUserModel.ItemCraft()`를 호출한 뒤 수행되는 구조입니다.

### Assistant

카테고리도 확인했는데, 해당 제작식들은 `CraftingCategoryId=50006`이고 로컬라이즈 키가 `Craft_NewReturn`/`신규/복귀`로 잡혀 있습니다. 즉 단순히 UI 이름만 있는 게 아니라, 제작식 필드로 서버 제한까지 연결되어 있습니다.

### Assistant

작은 주의점 하나: `CraftingCategoryId=50006`의 `MainCategoryMenu` 값은 56이라 enum상 `EventU2`입니다. 로컬라이즈에는 별도로 `Craft_60`/`Craft_NewReturn`도 있지만, 현재 JSON의 720xxx 제작식들이 그 60번 메뉴를 쓰는지는 추가로 바로 확인해 보겠습니다.

### Assistant

서버 소스 안에는 별도 `CraftingData.json` 복사본이 보이지 않아, 현재 확인한 데이터는 클라이언트 밸런스 JSON 기준입니다. 서버도 `LoaderCraftingData`를 통해 같은 구조의 `CraftingData.NewUserFlag`를 보므로, 배포 데이터가 이 값으로 들어오면 서버에서 그대로 막습니다.

### Assistant

서버 테스트 데이터에도 `CraftingData.json`이 따로 있네요. 같은 `NewUserFlag` 분포인지 서버 테스트 데이터까지 확인해서, 실제 서버 검증 결과와 데이터 기준을 분리해 정리하겠습니다.

### Assistant

확인 결과, **제작식에 신규/복귀 유저 제한 조건이 있습니다.**

핵심 로직은 [ActorUserModel_Inventory.cs](E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Inventory.cs:2723) 입니다. `CS_ItemCraft(401)` 처리 후 실제 제작 진입 시:

```csharp
CheckNewReturnUserFlag(craftingData.NewUserFlag, now)
```

검사에 실패하면 [SC_ItemCraft(402)](E:/Rasalas/Trunk/qa/Client/Assets/Script/SharedLib/Packet/Protocol/Item.cs:698)로 `NotAcceptNewReturnUser(-8043)`를 내려보내고 제작을 중단합니다.

`NewUserFlag` 의미는 [EventHubPageInfo.cs](E:/Rasalas/Trunk/qa/Client/Assets/Script/SharedLib/DataManager/DataClass/EventHubPageInfo.cs:43) 기준입니다.

- `0 None`: 제한 없음
- `1 NewUser`: 신규 유저만
- `2 ReturnUser`: 복귀 유저만
- `3 AllUser`: 신규 또는 복귀
- `4 NotNewReturnUser`: 신규/복귀가 아닌 유저만

클라이언트도 동일하게 사전 차단합니다. [UIUtil_Item.cs](E:/Rasalas/Trunk/qa/Client/Assets/Script/Util/UI/UIUtil_Item.cs:976), [UIScreenCraft.cs](E:/Rasalas/Trunk/qa/Client/Assets/Script/UI/Craft/UIScreenCraft.cs:1723)에서 `NewUserFlag`가 맞지 않으면 제작 버튼 흐름이 막힙니다.

데이터 기준으로는 [Client/Assets/0_BalanceData/GameData/CraftingData.json](E:/Rasalas/Trunk/qa/Client/Assets/0_BalanceData/GameData/CraftingData.json:15754)에 `NewUserFlag != 0` 제작식이 **54개** 있습니다.

- 신규 전용 `1`: 18개, `720001~720016`, `720033`, `720035`
- 복귀 전용 `2`: 18개, `720017~720032`, `720034`, `720036`
- 신규/복귀 아님 `4`: 18개, `720037~720054`
- `AllUser(3)` 제작식은 현재 클라 밸런스 JSON에는 없습니다.

단, 위 54개는 모두 제작 기간이 `2025-05-29`까지라서 현재 날짜 기준으로는 기간 조건에서도 막힙니다. 참고로 `Server/TestGameData` 쪽 `CraftingData.json` 두 개는 현재 `NewUserFlag` 비-0 제작식이 없었습니다.
