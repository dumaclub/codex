# 접속 유지 보상 테이블 확인

- Project: Rasalas
- Session ID: `019fea62-29d7-7b42-95e8-b7b02063051f`
- Workspace: `E:\Rasalas\Trunk\qa`
- Started: 2026-08-10T06:35:29.758Z
- Updated: 2026-08-10T06:36:14.5044727Z
- Messages exported: 5

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

# Files mentioned by the user:

## codex-clipboard-d3b20874-3754-4386-86cc-85b4157e2436.png: C:/Users/dumaclub/AppData/Local/Temp/codex-clipboard-d3b20874-3754-4386-86cc-85b4157e2436.png

## My request:
접속 유지 보상 이벤트는 어떤테이블을 읽어야 하나요? 현재 라이브에서 발생된 오류인데 시간이 경과함에 따라서 보상을 받아야 하는데 앞에보상을 받지 못했는데 쉬에 3개 보상을 받은 상태입니다.

<image name=[Image #1] path="C:\Users\dumaclub\AppData\Local\Temp\codex-clipboard-d3b20874-3754-4386-86cc-85b4157e2436.png">

</image>

### Assistant

라이브 오류의 데이터 흐름을 추적해 보겠습니다. 화면에 보이는 `UIStayConnectedData`, `UserEventPacket`, `ClearCount`를 출발점으로 클라이언트·서버·테이블 로딩 코드를 연결해서, 실제 참조 테이블과 “앞 보상 누락 후 뒤 3개 수령”이 가능한 조건을 확인하겠습니다.

### Assistant

정적 보상 구성은 `EventLoginKeep` 테이블이 맞습니다. 다만 라이브에서 “받은 기록”은 별도 DB 엔티티로 관리되므로, 지금부터는 테이블 설정 문제인지 저장/판정 로직 문제인지 분리해서 보겠습니다. 특히 화면의 `ClearCount = [120,150,180]`이 만들어지는 서버 경로를 확인 중입니다.

### Assistant

재현 증상과 코드가 맞물리는 지점이 보입니다. 보상 단계가 `15/30/45/60/90/120/150/180`인데, 중복수령 방지 캐시가 `(eventId, countTime)`이 아니라 `countTime` 하나만 키로 사용합니다. 그래서 과거/다른 접속유지 이벤트의 동일 시간 기록이 현재 이벤트의 앞 단계만 차단할 수 있습니다. 커밋 이력과 DB 적재 방식까지 확인해 확정도를 높이겠습니다.

### Assistant

현재 국내 라이브 기준으로는 다음을 확인해야 합니다.

- 이벤트 설정: [EventHubPageInfo.xlsx](E:/Rasalas/Trunk/qa/Table/EventHubPageInfo.xlsx)
  현재 접속 유지 이벤트는 `EventId=113001`, `EventType=3`, `ApplyAreaType=3(Account)`입니다.
- 단계별 시간/보상: [EventLoginKeep.xlsx](E:/Rasalas/Trunk/qa/Table/EventLoginKeep.xlsx)
  `15, 30, 45, 60, 90, 120, 150, 180분`과 `RewardConfirmId`를 읽습니다.
- 보상 아이템: [RewardConfirm.xlsx](E:/Rasalas/Trunk/qa/Table/RewardConfirm.xlsx) → `RewardGroup` → `RewardList`
- 유저의 화면 진행 상태: DB `accountevent`
- 중복 수령 방지 기록: DB `userevent_loginkeep`

라이브 DB에서는 우선 아래처럼 조회하는 것이 좋습니다.

```sql
-- 현재 접속 유지 이벤트 진행 상태
SELECT eventId, eventType, currentCount, clearCount, checkDate, updateDate
FROM accountevent
WHERE uId = :uId
  AND eventId = 113001;

-- 중복 수령 방지 기록
-- eventId로 먼저 필터링하지 않는 것이 중요합니다.
SELECT idx, eventId, countTime, checkDate, updateDate
FROM userevent_loginkeep
WHERE uId = :uId
ORDER BY countTime, checkDate DESC;
```

코드상 가장 유력한 원인은 `userevent_loginkeep` 처리 오류입니다.

- DB에서 해당 유저의 모든 접속 유지 기록을 읽습니다: [DatabaseController.cs](E:/Rasalas/Trunk/qa/Server/EgonServer/Controllers/DatabaseController.cs:1644)
- 그런데 메모리 캐시 키가 `(eventId, countTime)`이 아니라 `countTime` 하나뿐입니다: [UserEventModel.cs](E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/UserEventModel.cs:197)
- 보상 시에도 `eventId`를 확인하지 않고, 오늘 초기화 이후 동일 `countTime` 기록이 있으면 보상 후보에서 제거합니다: [중복 판정 코드](E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/UserEventModel.cs:2189)
- 추가로 `UserEventLoginKeepEntity.Copy()`에서 `eventId` 복사가 누락되어 있습니다: [UserEventEntity.cs](E:/Rasalas/Trunk/qa/Server/EgonServer/Database/Entities/UserEventEntity.cs:981)

따라서 `accountevent.clearCount`에는 화면처럼 `120,150,180`만 있는데, `userevent_loginkeep`에는 오늘 기준 `15,30,45,60,90` 기록이 남아 있다면 원인이 확정됩니다. 특히 그 행의 `eventId`가 `0`이거나 과거 이벤트 ID라면 위 결함에 정확히 해당합니다.

수정 방향은 다음 세 가지입니다.

1. 캐시 키를 `(eventId, countTime)` 복합키로 변경
2. 중복 판정 시 현재 `eventId`까지 비교
3. `UserEventLoginKeepEntity.Copy()`에 `this.eventId = info.eventId;` 추가

현재 화면의 `ClearCount=[120,150,180]`은 클라이언트 표시 문제가 아니라 서버가 실제로 뒤의 세 단계만 완료 처리해 전달한 상태입니다.
