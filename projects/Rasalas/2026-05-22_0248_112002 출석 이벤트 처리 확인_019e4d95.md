# 112002 출석 이벤트 처리 확인

- Project: Rasalas
- Session ID: `019e4d95-7184-7773-8183-9b885ce7ff2d`
- Workspace: `E:\Rasalas\Trunk\qa`
- Started: 2026-05-22T02:48:20.100Z
- Updated: 2026-05-22T02:48:32.1374202Z
- Messages exported: 25

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

<environment_context>
  <cwd>E:\Rasalas\Trunk\qa</cwd>
  <shell>powershell</shell>
  <current_date>2026-05-22</current_date>
  <timezone>Asia/Seoul</timezone>
</environment_context>

### User

출석 이벤트 확인 
EventHubPageInfo 데이터의 112002 이벤트 처리 과정을 알려주세요. 
[2,3차서버][출석 : 2026/03/18 - 2028/12/11]신규 서버 전설 펫 소환 출석		112002	112002
캡처 화면의 DB데이터를 확인해 보면 currentCount, clearCount, premiumCount 해당컬럼의 변경 조건을 알고 싶습니다. 

추가로 주간초기화 리셋 함수에 public void SetResetEvent(bool sendFlag) 위의 이벤트를 넣어도 정상동작하는지 확인하고 싶습니다.  


<image>

<image omitted>

</image>

### Assistant

코드에서 실제 처리 흐름을 따라가 보겠습니다. 우선 `EventHubPageInfo`, `AttendEvent`, `SetResetEvent`, 그리고 이벤트 ID `112002`가 어디서 쓰이는지 좁혀볼게요.

### Assistant

전체 검색은 Unity `Library/Temp` 쪽까지 물고 들어가서 너무 커졌습니다. 서버 로직과 게임데이터 쪽으로 범위를 줄여서 다시 따라가겠습니다.

### Assistant

관련 지점이 서버의 `UserEventModel`과 `UserEventEntity`로 모였습니다. 이제 실제 출석 체크, 보상 수령, 리셋 로직을 라인 단위로 열어보겠습니다.

### Assistant

`EventHubPageInfo.json`의 현재 테스트 데이터에서는 `112002`가 바로 잡히지 않아, 테이블 원본/서버 빌드 데이터까지 포함해서 이벤트 정의와 보상/출석 체크 데이터를 더 확인하겠습니다.

### Assistant

`112002`는 현재 `EventHubPageInfo.json`보다 테이블 원본/생성 산출물 쪽에 있는 것으로 보입니다. 출력이 너무 많아졌으니 이번엔 관련 테이블 파일명만 좁혀서 정확한 행을 보겠습니다.

### Assistant

이벤트 정의가 JSON 산출물에는 없고 `Table\EventHubPageInfo.xlsx`에 열려 있는 변경분처럼 보입니다. 엑셀 테이블을 직접 읽어서 `112002` 행과 출석 보상 그룹을 확인하겠습니다.

### Assistant

`spreadsheets` 스킬을 잠깐 사용해 엑셀 테이블을 안전하게 읽겠습니다. 코드 흐름 분석은 그대로 서버 소스 기준으로 이어갑니다.

### Assistant

엑셀 기준으로는 `112002`가 출석 이벤트이고, `AttendCheckGroup=11202`, 실제 DB `premiumCount` 초깃값은 `112021`로 계산되는 구조입니다. 다만 서버가 실제 로드하는 JSON 산출물에 반영됐는지도 따로 확인하겠습니다.

### Assistant

확인 결과, 캡처는 `fromServerId` 컬럼이 있어서 `userevent`가 아니라 `accountevent` 데이터로 보입니다. `EventHubPageInfo.xlsx` 기준으로 `112002`는 `EventType=2(EventCheck/출석)`, `ApplyAreaType=3(Account)`, 서버그룹 `710,720,730,740,810,820,830`, 기간 `2026-03-18 ~ 2028-12-11` 이벤트입니다.

**컬럼 변경 조건**
- `currentCount`
  - `CheckAttendEventAll()`에서 활성 출석 이벤트를 찾고, 오늘 아직 출석 처리하지 않았고 `clearCount`가 비어 있으면 `IncAttendCount()`가 호출됩니다.
  - 이때 `currentCount++` 됩니다.
  - 보상을 안 받은 상태, 즉 `clearCount`가 남아 있으면 다음 출석 증가가 막힙니다.
  - 보상 테이블 최대 일수를 넘어도 `clearCount`가 비어 있으면 계속 증가할 수 있습니다. 캡처의 `112001 currentCount=36`은 이런 케이스로 보입니다.

- `clearCount`
  - 출석 증가 시 `AttendCheckData`에서 현재 일차에 받을 수 있는 보상 ID가 들어갑니다.
  - `112002`는 `AttendCheckGroup=11202`, 실제 그룹 `112021`, 보상 ID는 `111015 ~ 111044`로 30일 구성입니다.
  - 예: 1일차 출석 후 `clearCount=111015,`
  - 보상 수령 `GiveAttendReward()` 후에는 `RemoveClearCount(attendId)`로 해당 ID가 제거됩니다.
  - 그래서 이 출석 이벤트에서 `clearCount`는 “수령 완료 목록”이라기보다 “현재 수령 가능한 미수령 보상 ID 목록”에 가깝습니다.

- `premiumCount`
  - 최초 생성 시 0일 수 있고, 첫 출석 처리 때 `AttendCheckConfig.EventId=112002 -> AttendCheckGroup=11202`를 기준으로 `AttendCheckGroupToFirstGroup(11202)`가 적용되어 `112021`이 됩니다.
  - 캡처의 `112001 premiumCount=112011`은 `112001` 이벤트의 그룹값이고, `112002`라면 정상 기대값은 `112021`입니다.
  - `112002`는 `RunningTime=0`, `NextAttendCheckGroup=0`이라 일반 흐름에서는 이후 그룹 변경이 없습니다.

관련 코드 흐름은 [UserEventModel.cs](/E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/UserEventModel.cs:1605), [UserEventModel.cs](/E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/UserEventModel.cs:1492), [UserEventModel.cs](/E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/UserEventModel.cs:1686), [LoaderAttendCheckData.cs](/E:/Rasalas/Trunk/qa/Client/Assets/Script/SharedLib/DataManager/Loader/LoaderAttendCheckData.cs:328) 쪽입니다.

**주간 리셋에 넣는 건?**
현재 상태 그대로는 정상 동작하지 않을 가능성이 큽니다.

`SetResetEvent(bool sendFlag)`는 `EventHubPageInfo.ResetType > 0`인 이벤트를 대상으로 삼지만, 실제 리셋 대상은 `EventMissionData`에서 `resetEvent.EventId`에 매핑된 `EventMission.Id` 목록입니다. `112002`는 출석 이벤트라서 `EventMissionData` 대상이 아니므로, `ResetType=2`만 넣어도 리셋할 user event를 찾지 못합니다. 관련 코드는 [UserEventModel.cs](/E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/UserEventModel.cs:201), [UserEventModel.cs](/E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/UserEventModel.cs:293)입니다.

주간 출석 리셋으로 쓰려면 `SetResetEvent()`에 `EventType == EventCheck`일 때 `resetEvent.EventId` 자체를 `_userEventDic`에서 찾아 `ResetEvent()` 하도록 별도 분기가 필요합니다. `ResetEvent()`는 `currentCount=0`, `clearCount` 초기화, `checkDate` 갱신만 하고 `premiumCount`는 유지합니다. 즉 `112002`는 리셋 후에도 `premiumCount=112021` 유지가 맞습니다.

추가로 중요한 점 하나: 현재 작업트리 기준으로 `Table\EventHubPageInfo.xlsx`, `Table\AttendCheckData.xlsx`에는 `112002`가 있지만, 서버가 로드하는 `Server\TestGameData\Data\GameData\EventHubPageInfo.json` / `AttendCheckData.json`에서는 `112002`, `112021`이 검색되지 않았습니다. 실제 서버 반영 전에는 테이블 JSON/바이너리 생성 산출물 갱신이 먼저 필요합니다.

### User

주간 리셋은 이벤트 타입별로 다르게 처리하려고 합니다. 
namespace SharedLib.Data
{
    public enum eEventType
    {
        EventPass = 1,
        EventCheck = 2,
        EventLoginKeep = 3,
        EventExchange = 4,
        EventMission = 5,
        EventDailyMission = 6,
        EventDailyCash = 7,
        EventDailyMarion = 8,
        EventDgnTmDouble = 9,
        EventGuildGive = 10,
        EventCashAttend = 11,
        EventNPC = 12,
        EventBanner = 13,
        EventNewUser = 14,
        EventReturnUser = 15,
        EventGuildDungeon = 16,
        EventPrivilegeAttend = 17,      //특권 출석

#if UNITY_CLIENT
        //클라 전용
        EventBannerClientOnly = 101,
#endif
    }
기존에 주간 초기화 이벤트는 5번만 있었고 2번은 DB에서 삭제 후 _userEventDic 에서 제거하는 방식으로 진행하려고 하는데 어떤가요? 
출석이벤트 같은 경우에는 틱별로 체크하여 문제될 수 있는 지 확인이 필요합니다.

### Assistant

좋은 방향입니다. 다만 출석은 매 틱 체크가 걸려 있어서 “삭제 후 딕셔너리 제거”가 곧바로 “새 이벤트 생성 + 당일 출석 1회 처리”로 이어지는지까지 봐야 합니다. 그 부분을 코드 기준으로 한 번 더 짚어볼게요.

### Assistant

방향 자체는 괜찮습니다. 다만 **“DB 삭제 후 `_userEventDic` 제거”는 저장 흐름과 틱 재생성 흐름을 같이 맞춰야 안전**합니다.

핵심은 이겁니다.

`EventCheck(2)` 출석은 `ActorUserModel.Update()`에서 매 틱 `CheckAttendEventAll(true)`가 돕니다. 그래서 주간 리셋 때 `_userEventDic`에서 112002를 제거하면, 다음 틱에:

1. `CheckAttendEventAll()`
2. 활성 `EventCheck` 목록에서 112002 발견
3. `_userEventDic`에 없으므로 `GetEventBaseEntity()`가 새 엔티티 생성
4. `updateDate`가 어제 값으로 초기화되어 daily 체크 통과
5. `IncAttendCount()` 호출
6. 바로 `currentCount=1`, `premiumCount=112021`, `clearCount=111015,` 상태가 됩니다

즉 “삭제 후 제거”는 **리셋 직후 새 시즌 1일차 출석이 자동으로 잡히는 방식**입니다. 이게 기획 의도라면 정상입니다. 단, “유저가 팝업을 열 때만 출석 처리”가 의도라면 현재 구조와는 맞지 않습니다. 이미 출석 체크가 틱/유저 이벤트 리스트 요청/접속 흐름에서 자동으로 돌고 있습니다.

주의할 점은 DB 저장입니다. 기존 `SaveToDB()`는 `_userEventDic.Values`만 순회합니다. 그래서 단순히:

```csharp
eventData.setDeleteEvent();
_userEventDic.TryRemove(eventId, out _);
```

이렇게 하면 삭제 상태 엔티티가 저장 루프에서 빠질 수 있습니다. `setDeleteEvent()` 안에서 `UpdateEntity()`로 Redis 갱신은 타지만, DB 반영 경로까지 확실히 보장하려면 삭제 엔티티를 별도 pending 리스트에 보관하거나, DB 삭제/저장 처리가 끝난 뒤 딕셔너리에서 제거하는 구조가 안전합니다.

추천 구조는:

- `EventMission(5)`: 기존처럼 미션 ID 목록 기준 `ResetEvent()` 또는 타입별 기존 처리 유지.
- `EventCheck(2)`: `resetEvent.EventId` 자체를 대상으로 처리.
  - 주간 출석을 완전 신규 시즌처럼 시작하려면 삭제 마킹 + DB 저장 보장 + `_userEventDic` 제거.
  - 단순 진행도만 초기화하려면 `ResetEvent(now)`가 더 안전합니다. 이 경우 `premiumCount`는 유지되고 `currentCount/clearCount/checkDate`만 초기화됩니다.

개인적으로는 `112002` 같은 단일 30일 출석을 매주 다시 1일차부터 시작시키는 목적이면 **삭제보다 `ResetEvent()` 확장 처리가 더 덜 위험**해 보입니다. 삭제 방식은 “다음 틱 즉시 재생성” 자체는 문제 없지만, 삭제 저장 누락과 동일 틱/동일 저장 주기에서 insert-delete 순서 문제가 생길 여지가 있습니다.

결론: 타입별 분기는 찬성입니다. 다만 `EventCheck(2)`를 삭제 방식으로 갈 거면 “삭제 엔티티 저장 보장”을 먼저 넣고, 리셋 직후 `CheckAttendEventAll()`이 즉시 1일차 보상 대기 상태를 만드는 것이 의도인지 기획 확인이 필요합니다.

### User

추천하시는 방법으로 주간 초기화 작업을 수정해 주세요. case 로 이벤트 타입도 분기해주세요.

### Assistant

알겠습니다. 제가 추천드린 쪽, 즉 `EventCheck(2)`는 삭제 재생성 대신 `ResetEvent()`로 진행도만 초기화하는 방식으로 넣겠습니다. 기존 `EventMission(5)` 동작은 유지하면서 `switch/case`로 타입별 처리 경계를 만들게요.

### Assistant

한 가지 더 짚고 수정하겠습니다. 출석은 `updateDate`가 당일 출석 여부 게이트라서 단순 `ResetEvent()`만 쓰면 특정 타이밍에서 리셋 직후 1일차 출석이 막힐 수 있습니다. 그래서 `EventCheck` 전용 리셋은 진행도 초기화와 함께 `updateDate`를 전일로 밀어, 다음 틱의 출석 체크가 자연스럽게 1일차를 생성하도록 하겠습니다.

### Assistant

이제 실제 수정에 들어갑니다. 변경은 두 곳입니다: `SetResetEvent()`의 타입별 분기와, 출석 리셋 전용으로 `updateDate`까지 조정하는 엔티티 메서드 추가입니다.

### Assistant

수정은 들어갔습니다. 이제 문법과 참조가 맞는지 확인하고, 가능하면 서버 솔루션 빌드까지 돌려보겠습니다.

### Assistant

빌드는 통과했습니다. 기존 패키지 취약성/소스링크/숨김 멤버 같은 기존 경고는 많이 나오지만, 이번 변경으로 인한 컴파일 오류는 없습니다.

### Assistant

수정 완료했습니다.

[UserEventModel.cs](/E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/UserEventModel.cs:201)에서 `SetResetEvent()`가 `switch (resetEvent.EventType)`로 분기되도록 바꿨습니다.

- `EventMission(5)`: 기존처럼 `EventMissionData` 목록을 찾아 각 미션 엔티티를 초기화합니다.
- `EventCheck(2)`: `resetEvent.EventId` 자체의 `AttendEvent` 엔티티를 찾아 출석 전용 초기화를 합니다.
- 그 외 타입: 현재는 처리하지 않고 skip합니다.

출석 전용 초기화는 [UserEventEntity.cs](/E:/Rasalas/Trunk/qa/Server/EgonServer/Database/Entities/UserEventEntity.cs:396)에 `ResetAttendEvent()`로 추가했습니다. `currentCount=0`, `clearCount` 초기화, `checkDate` 갱신은 동일하고, `updateDate`를 하루 전으로 밀어 리셋 직후 다음 틱의 `CheckAttendEventAll()`에서 1일차 출석이 정상 생성되도록 했습니다. `premiumCount`는 유지됩니다.

검증도 했습니다. `.\Server\Server.sln` Debug x64 빌드 통과했습니다. 기존 경고들은 남아 있지만 이번 변경으로 인한 컴파일 오류는 없습니다.

### User

<environment_context>
  <current_date>2026-06-03</current_date>
  <timezone>Asia/Seoul</timezone>
</environment_context>

### User

현재 출석 이벤트 초기화 기능이 들어가 있나요?

### Assistant

네, 방금 반영한 코드 기준으로 다시 확인해 보겠습니다. `EventCheck(출석)` 분기와 출석 전용 리셋 메서드가 실제 파일에 남아 있는지 빠르게 검증할게요.

### Assistant

현재 상태로는 **출석 이벤트 초기화 기능이 코드에는 준비돼 있지만, 실제로 동작하지는 않습니다.**

확인한 내용:

- [UserEventEntity.cs](/E:/Rasalas/Trunk/qa/Server/EgonServer/Database/Entities/UserEventEntity.cs:396)에 `ResetAttendEvent()`는 들어가 있습니다.
  - `currentCount = 0`
  - `clearCountList.Clear()`
  - `updateDate = time.AddDays(-1)`
  - `checkDate = time`
  - `UpdateEntity()`

- 그런데 [UserEventModel.cs](/E:/Rasalas/Trunk/qa/Server/EgonServer/ServerNodes/GameNode/Models/UserEventModel.cs:239)의 `switch (resetEvent.EventType)`에서 `EventCheck` 분기가 주석 처리되어 있습니다.

```csharp
//case eEventType.EventCheck:
//    ResetAttendCheckEvent(resetEvent, resetTm, nowTm, sendFlag);
//    break;
```

그래서 현재 주간 초기화에서 실제로 실행되는 건 `EventMission(5)`만이고, `EventCheck(2)` 출석 이벤트는 리셋 호출이 안 됩니다. 출석 초기화를 활성화하려면 해당 `case eEventType.EventCheck` 주석을 풀어야 합니다.
