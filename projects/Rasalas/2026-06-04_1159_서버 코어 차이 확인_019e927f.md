# 서버 코어 차이 확인

- Project: Rasalas
- Session ID: `019e927f-d85e-74d0-ae2d-6ce6a48a1a3c`
- Workspace: `E:\Rasalas\Trunk\qa`
- Started: 2026-06-04T11:58:32.542Z
- Updated: 2026-06-04T11:59:46.6455744Z
- Messages exported: 20

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

라살라스 서버는 500명 정도 같이 게임을 하다보면 게임서버에 렉이 발생하여 게임이 원할하지 않다는데 아스텔리아 서버는 그런 현상이 없다고 합니다. 서버 코어중에 어떤 차이가 있기 때문에 그런 것인지 확인해 주세요.

### Assistant

우선 서버 쪽 구조와 “코어”가 어디에 묶여 있는지부터 잡아보겠습니다. 비교 대상인 아스텔리아 코드가 같은 워크스페이스에 있으면 함께 대조하고, 없으면 라살라스 내부에서 병목을 만들 만한 서버 코어 설정/루프/스레딩 차이를 먼저 추적하겠습니다.

### Assistant

라살라스 쪽은 `Server\EgonServer`와 `Server\Engine\K2.Core`가 핵심으로 보입니다. 이제 아스텔리아 이름/공통 엔진 흔적과 서버 루프, 타이머, 네트워크 큐 설정을 좁혀서 보겠습니다.

### Assistant

E: 드라이브에 `ProjectA02`가 보입니다. 이름상 아스텔리아 계열일 가능성이 높아서, 라살라스 `Server\EgonServer`와 대응되는 서버 프로젝트/코어 파일이 있는지 확인해 보겠습니다.

### Assistant

아스텔리아 쪽은 `E:\ProjectA02\Trunk\Dev\Server\EgonServer`로 대응됩니다. 먼저 공통 엔진(`K2.Core`)이 서로 같은지 확인하고, 다르면 디스패처/백그라운드 작업/타이머 쪽을 우선 보겠습니다.

### Assistant

`K2.Core`의 표면 구조는 거의 같지만 출력 폭 때문에 길이가 제대로 보이지 않았습니다. 해시 기준으로 소스 파일만 비교해서 실제 차이가 있는지 정확히 분리하겠습니다.

### Assistant

첫 번째로 중요한 신호가 나왔습니다. `K2.Core`에서 실제 차이는 `JobDispatcher`, `JobMessage`, `WheelTimer`, `K2.Core.csproj` 네 개뿐이라서, 이 중 서버 랙과 직접 연결될 수 있는 디스패처/타이머 코드를 열어보겠습니다.

### Assistant

확실한 후보가 하나 잡혔습니다. 라살라스는 `JobDispatcher` 기본값이 히스토리 수집 ON이고, 아스텔리아는 OFF입니다. 이게 액터/유저마다 붙는 디스패처라면 500명 구간에서 큐 처리와 메모리 압박 차이를 만들 수 있어서 사용처를 확인하겠습니다.

### Assistant

사용처도 확인되었습니다. 특히 라살라스 `ActorUserModel`은 `new JobDispatcher()`라 기본값을 그대로 타고 있고, 현재 라살라스 기본값은 히스토리 ON입니다. 세션과 DB 로그 큐도 같은 패턴인지 더 확인해 병목 가능성을 분류하겠습니다.

### Assistant

히스토리 기본값 외에도 스레드 모델/게이트웨이 워커/존 업데이트 루프 차이가 있습니다. 이건 “500명 동접 시 게임 서버 렉”과 더 직접 맞닿아 있으니 해당 파일들의 diff를 보겠습니다.

### Assistant

`DBNodeLogTaskManager` 차이는 주로 대기열 경고 로그 추가/주석 코드 정리라 직접 부하 원인이라기보다는 관측성 차이에 가깝습니다. 반면 `WheelTimer`와 `JobDispatcher`, `ThreadModel_Zone`은 실제 실행 루프/큐 동작 차이라 계속 우선순위에 두겠습니다.

### Assistant

아스텔리아 `ThreadModel_Zone`에서는 `_threadIdx == 0`의 매 프레임 호출에서 `UpdateGlobalDungeonReset()`이 빠져 있고, 라살라스에는 들어 있습니다. 이 함수가 무겁거나 전체 유저/존을 훑는다면 “게임서버 렉”의 두 번째 유력 후보입니다. 함수 본문을 확인하겠습니다.

### Assistant

아스텔리아에도 월드 던전 리셋 함수는 있지만, 검색 결과상 존 루프에서 매 틱 호출되지는 않습니다. 라살라스는 이 로직이 `ZoneManager` 안으로 들어와 `_threadIdx == 0`에서 매 업데이트마다 실행되는 구조라 본문 비용을 열어보겠습니다.

### Assistant

이 부분은 구조 차이가 명확합니다. 아스텔리아는 `WorldDungeonManager.UpdateGlobalDungeonReset()`을 커뮤니티 워커 스레드에서 처리하고, 라살라스는 게임 존 스레드 0번에서 처리합니다. 이제 실제 틱 주기와 존 스레드 개수를 봐서, 이 차이가 어느 정도 빈도로 발생하는지 확인하겠습니다.

### Assistant

`WheelTimer`의 아스텔리아 수정은 단순 취향 차이가 아닙니다. 라살라스 방식은 `headIndex`가 버킷 크기보다 커지는 경우를 한 번만 보정하고, 아스텔리아는 매번 modulo로 고정합니다. 타이머 콜백이 지연/겹침을 겪으면 라살라스가 타이머 예약 위치를 잘못 잡을 수 있어 이 부분도 핵심 후보로 남깁니다.

### Assistant

`GlobalManager.Timer`는 유저 모델의 DB 저장 반복 타이머에도 쓰입니다. 즉 500명 기준으로 반복 예약이 꽤 많이 걸리는 구조라, `WheelTimer`의 버킷 인덱스 보정 차이는 실제 부하/지연 때 체감될 수 있습니다.

### Assistant

이제 프로젝트 설정 차이도 한 번 확인하겠습니다. 조건부 컴파일 심볼이 달라지면 방금 본 `#if` 블록의 실제 실행 경로가 달라질 수 있어서, `EgonServer.csproj` 설정까지 같이 봐야 결론이 단단해집니다.

### Assistant

핵심 후보는 거의 좁혀졌고, 최종 답변 전에 라인 번호와 비교 지점을 한 번 더 확인하겠습니다. 특히 패킷 송신 경로와 디스패처 기본값은 부하 때 바로 체감될 수 있는 차이라 정확히 짚어볼게요.

### Assistant

추가 확인 결과, 라살라스는 송신 패킷마다 `ThreadPool.QueueUserWorkItem`을 한 번 더 거치는 구조이고 아스텔리아는 그 단계를 제거해 둔 상태입니다. 500명 동시 플레이에서 이 차이는 꽤 직접적인 렉 후보로 보입니다.

### Assistant

비교 대상은 `E:\ProjectA02\Trunk\Dev`를 아스텔리아 서버로 보고 확인했습니다. 코드 차이 기준으로는 **라살라스 렉의 1순위 후보가 꽤 명확합니다.**

가장 큰 차이는 라살라스의 패킷 송신 경로입니다. 라살라스는 `SendPacket()`에서 매번 `BackgroundJob.Execute()`를 타고 다시 `PostPacket()`을 호출합니다: [Session.cs](<E:\Rasalas\Trunk\qa\Server\EgonServer\Server\Session.cs:273>), [Session.cs](<E:\Rasalas\Trunk\qa\Server\EgonServer\Server\Session.cs:284>). `BackgroundJob.Execute()`는 내부적으로 `ThreadPool.QueueUserWorkItem`입니다: [BackgroundJob.cs](<E:\Rasalas\Trunk\qa\Server\Engine\K2.Core\Util\BackgroundJob.cs:16>). 반면 아스텔리아는 바로 `PostPacket()`을 호출합니다: [Session.cs](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\Server\Session.cs:288>), [Session.cs](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\Server\Session.cs:299>).
즉 라살라스는 **송신 패킷마다 ThreadPool 작업을 하나 더 생성**합니다. 500명 전투/이동/동기화 상황에서는 이게 ThreadPool 큐 적체와 컨텍스트 전환 증가로 바로 렉으로 이어질 수 있습니다.

두 번째는 `JobDispatcher` 기본값입니다. 라살라스는 `useHistory = true`: [JobDispatcher.cs](<E:\Rasalas\Trunk\qa\Server\Engine\K2.Core\Messaging\JobDispatcher.cs:35>), 아스텔리아는 `useHistory = false`: [JobDispatcher.cs](<E:\ProjectA02\Trunk\Dev\Server\Engine\K2.Core\Messaging\JobDispatcher.cs:31>). 라살라스에서는 `ActorUserModel`이 기본 생성자를 쓰기 때문에 유저 액터마다 히스토리 기록 비용이 붙습니다: [ActorUserModel.cs](<E:\Rasalas\Trunk\qa\Server\EgonServer\ServerNodes\GameNode\Models\Actor\ActorUserModel.cs:44>). `DateTime.Now`와 순환 버퍼 기록이 잡마다 들어가므로 다수 유저 부하에서 CPU/GC 압박을 키울 수 있습니다.

세 번째는 타이머 코어입니다. 라살라스 `WheelTimer`는 `headIndex` 보정이 약하고, 아스텔리아는 매 tick/reserve마다 modulo로 안전하게 보정합니다: [WheelTimer.cs](<E:\Rasalas\Trunk\qa\Server\Engine\K2.Core\Timer\WheelTimer.cs:48>), [WheelTimer.cs](<E:\ProjectA02\Trunk\Dev\Server\Engine\K2.Core\Timer\WheelTimer.cs:48>). 부하로 타이머 콜백이 밀리거나 겹치면 라살라스 쪽이 예약 버킷이 흔들릴 가능성이 더 큽니다.

네 번째는 글로벌 던전 리셋 체크 위치입니다. 라살라스는 게임 Zone thread에서 매 업데이트 호출합니다: [ThreadModel_Zone.cs](<E:\Rasalas\Trunk\qa\Server\EgonServer\ServerNodes\GameNode\Thread\ThreadModel_Zone.cs:417>). 아스텔리아는 Community worker 쪽으로 빠져 있습니다: [ThreadModel_CommunityWorker.cs](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\CommunityNode\Thread\ThreadModel_CommunityWorker.cs:64>). 평소 비용은 작아도 상태 전환 시 게임 스레드에서 스파이크가 날 수 있습니다.

정리하면, 우선순위는 **1. `Session.SendPacket`의 불필요한 `BackgroundJob` 제거**, **2. `JobDispatcher` 기본 `useHistory=false`로 변경**, **3. 아스텔리아 `WheelTimer` 방식 반영**, **4. `UpdateGlobalDungeonReset()`을 게임 Zone thread 밖으로 이동/스로틀링**입니다. 이번 확인은 정적 코드 비교 기준이고, 실제 부하 테스트는 아직 수행하지 않았습니다.
