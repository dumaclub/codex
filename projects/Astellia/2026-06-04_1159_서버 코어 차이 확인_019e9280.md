# 서버 코어 차이 확인

- Project: Astellia
- Session ID: `019e9280-e774-7950-b1c6-10b77621a0a3`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-06-04T11:59:41.941Z
- Updated: 2026-06-04T12:00:01.322576Z
- Messages exported: 21

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

라살라스 서버는 500명 정도 같이 게임을 하다보면 게임서버에 렉이 발생하여 게임이 원할하지 않다는데 아스텔리아 서버는 그런 현상이 없다고 합니다. 서버 코어중에 어떤 차이가 있기 때문에 그런 것인지 확인해 주세요.

### Assistant

먼저 서버 코어가 어떻게 배치되어 있는지부터 훑어보겠습니다. 라살라스와 아스텔리아가 같은 저장소/브랜치/폴더 안에 있는지 확인한 뒤, 성능에 직접 영향을 줄 만한 루프, 네트워크, 타이머, DB/락 처리 차이를 좁혀볼게요.

### Assistant

확인해보니 이 작업 폴더는 Git 저장소가 아니라 SVN 작업본이고, 핵심은 `Server` 쪽에 있을 가능성이 큽니다. 이제 프로젝트명/솔루션명과 “Astellia/Rasalas” 흔적을 찾아서 비교 대상이 같은 트리 안에 있는지 보겠습니다.

### Assistant

검색 결과에 클라이언트/테이블 자산이 많이 섞여서 노이즈가 컸습니다. 서버만 다시 좁히고, 특히 `EgonServer`, `EgonCommon`, `SuperSocketLite` 쪽의 코어 구현과 빌드 설정을 보겠습니다.

### Assistant

`EgonServer.csproj`에서 이미 라살라스 전용 흔적(`com.itoxi.rasalas`, `E:\Rasalas...`)과 여러 빌드 구성(`Debug_GC`, `INTER_DEBUG`, `Debug_TW`)이 보입니다. 비교 대상인 아스텔리아 서버 코어가 같은 머신의 다른 작업본에 있을 수 있어서, 로컬 드라이브의 상위 폴더와 SVN 정보를 확인해보겠습니다.

### Assistant

비교 대상이 보입니다. 현재 작업본은 `Project_A`이고, 로컬에 `D:\Astellia_M`가 따로 있으니 이제 두 서버 코어의 구조와 핵심 파일을 1:1로 맞춰보겠습니다.

### Assistant

`D:\Astellia_M`는 소스가 아니라 Unity 빌드 산출물이라 서버 코어 비교에는 부적합합니다. 대신 `D:\ProjectA`와 `E:\Rasalas*` 작업본에 서버 소스가 있는지 확인해보겠습니다.

### Assistant

`D:\ProjectA\Trunk`가 아스텔리아 쪽 소스일 가능성이 높아 보입니다. SVN URL과 서버 프로젝트명을 확인하고, 현재 `E:\ProjectA02`의 라살라스 서버와 같은 계층의 코어 파일을 비교하겠습니다.

### Assistant

`D:\ProjectA\Trunk\Dev` 아래에 서버 소스가 있습니다. 이전 검색은 클라이언트 `Library`까지 들어가서 오래 걸렸으니, 이제는 `Server`만 대상으로 줄여서 프로젝트 설정과 핵심 루프를 비교하겠습니다.

### Assistant

두 작업본 모두 같은 SVN 저장소(`Project_A`)의 다른 리비전입니다. 단순 설정은 거의 같지만 라살라스 쪽이 더 최신이고(`34312` vs `33362`), 이제 실제로 달라진 서버 코어 파일을 해시로 추려서 “성능에 닿는 차이”만 골라보겠습니다.

### Assistant

해시 비교 결과가 꽤 선명합니다. 네트워크/타이머/잡큐 같은 공통 코어(`Program.cs`, `EgonCommon`, `Engine`, `SuperSocketLite`, `GameNode\Thread`)는 실질 코드 차이가 없고, 차이는 `ServerModule`, `ZoneModel`, 일부 Game/Gateway 매니저에 몰려 있습니다. 이제 그 변경 내용이 500명 부하에서 렉을 만들 수 있는지 diff로 보겠습니다.

### Assistant

차이 규모는 크지 않습니다. 흥미로운 점은 라살라스 쪽이 대부분 “추가 코드”이고, 특히 `ScheduleGameManager`와 `ZoneModel`에 새 주기 작업/존 처리 코드가 들어간 형태입니다. 렉 원인 후보가 될 수 있는 주기 루프, 전체 유저/존 순회, 브로드캐스트 추가분을 한 번씩 열어보겠습니다.

### Assistant

추가로 보스 던전 딜미터 코드가 `ActorMonsterModel_Boss.cs` 쪽에 들어가 있습니다. 이건 500명 상황에서 꽤 중요할 수 있어서, `EgonServer` 전체 소스 차이를 다시 추려 빠진 파일이 없는지 확인하겠습니다.

### Assistant

`EgonServer` 전체로 보면 103개 파일이 달라졌지만, 대부분은 신규 컨텐츠/프로토콜/DB 엔티티입니다. 부하 관점에서 눈에 띄는 건 `ActorMonsterModel_Boss.cs`가 라살라스에만 추가됐고, 기존 `ActorMonsterModel`, `ActorModel`, `BattleHelper`, `SkillModel` 등에 그 호출부가 붙은 점입니다. 딜미터/보스던전 처리 흐름을 더 확인하겠습니다.

### Assistant

딜미터 구현을 보면 매 타격마다 누적 데미지를 기록하고, 변경분이 있으면 다음 몬스터 업데이트에서 존 전체 유저에게 패킷을 보냅니다. 이 부분은 인원수와 타격 빈도에 비례해서 커지는 구조라, 실제 렉 증상과 맞물릴 가능성이 큽니다. 호출 경로를 정확히 따라가겠습니다.

### Assistant

호출 경로가 잡혔습니다: 데미지 계산 후 `ActorModel.AddDealMeter()`에서 `OnPveBossDamage()`가 매번 호출되고, 몬스터 `OnActiveUpdate()`에서 변경분을 브로드캐스트합니다. 이제 이 업데이트가 어느 주기로 돌고 몇 명에게 전송되는지, 존 스레드의 루프를 보겠습니다.

### Assistant

존 스레드는 `K2Const.TickChannelUpdate` 주기로 모든 존의 모든 액터를 업데이트합니다. 즉 딜미터 훅은 별도 백그라운드 작업이 아니라 게임 존 업데이트 경로 위에 있어, 브로드캐스트 비용이 커지면 그대로 `delay update tick` 로그와 체감 렉으로 이어질 수 있습니다. 정확한 라인 번호와 관련 설정을 정리하겠습니다.

### Assistant

게이트웨이 브로드캐스트도 확인 중입니다. 주석은 “인게임 유저”라고 되어 있지만 실제 `BroadcastPacket` 구현이 로비까지 포함하는지, 모든 세션을 도는지에 따라 순간 부하가 달라집니다.

### Assistant

비교는 거의 끝났고, 마지막으로 “전체 게이트웨이 브로드캐스트”가 실제로 어디까지 뿌리는지 한 군데만 확인해 볼게요. 이게 주범인지 보조 스파이크인지 가르는 작은 퍼즐 조각입니다.

### Assistant

확인해 보니 게이트웨이 브로드캐스트는 연결된 인게임 유저 전체 목록을 만들어 순회합니다. 다만 호출 위치상 상시 틱 부하는 아니고, 보스 시간 알림 같은 이벤트성 부하라서 “계속 렉” 쪽은 보스 딜미터 갱신 경로가 더 유력합니다.

### Assistant

확인 결과, **아스텔리아와 라살라스의 저수준 서버 코어 자체는 거의 차이가 없습니다.**
`ThreadModel_Zone`, `EgonCommon`, `K2.Core`, `SuperSocketLite`, GC 설정, x64 설정 등은 의미 있는 차이가 없고, 렉 원인은 코어 엔진 차이라기보다 **라살라스에 추가된 보스 던전/딜미터 게임 로직** 쪽이 가장 유력합니다.

가장 의심되는 경로는 이 부분입니다.

- [ActorModel.cs](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\Actor\ActorModel.cs:2299>)
  데미지 누적 시마다 `OnPveBossDamage(attacker, damage)`가 호출됩니다.

- [ActorMonsterModel_Boss.cs](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\Actor\ActorMonsterModel_Boss.cs:186>)
  라살라스에만 있는 보스 던전 로직입니다. 데미지 발생마다 유저별 딜량 딕셔너리를 갱신하고, 딜미터 브로드캐스트 플래그를 세웁니다.

- [ActorMonsterModel_Boss.cs](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\Actor\ActorMonsterModel_Boss.cs:291>)
  딜미터 패킷을 만든 뒤 `zone.GetUserList()` 전체에게 `SendPacket` 합니다.

즉 500명이 같은 보스를 때리면, 매 데미지마다 보스 딜량 자료구조가 갱신되고, 다음 액터 업데이트에서 변경된 딜미터 정보가 존 유저 전체에게 뿌려집니다. 변경 유저 수와 접속 유저 수가 같이 커지기 때문에 부하가 `500명 전투`에서 급격히 커지는 구조입니다. 이 처리는 [ZoneModel.Update](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\Zone\ZoneModel.cs:1141>)의 액터 업데이트 루프 안쪽으로 이어지므로, 실제 게임 틱 지연으로 번질 수 있습니다.

보조 의심 지점도 있습니다.

- [ActorMonsterModel_Boss.cs](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\Actor\ActorMonsterModel_Boss.cs:391>)
  보스 종료/만료 시 전체 참가자 정렬, 보상 계산, 인벤토리/아바타/펫 조건 검사, 참가자별 로그 기록, 결과 패킷 발송이 한 번에 몰립니다. 지속 렉보다는 순간 스파이크 가능성이 큽니다.

- [RepSessionHandler.cs](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GatewayNode\PacketHandler\RepSessionHandler.cs:773>) / [GatewayUserManager.cs](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GatewayNode\Managers\GatewayUserManager.cs:232>)
  보스 던전 시간 알림은 게이트웨이에 연결된 인게임 유저 전체를 순회합니다. 다만 이벤트성이라 상시 렉의 1순위는 아닙니다.

결론적으로 “서버 코어 차이”라면 저수준 코어가 아니라, **라살라스 서버 코어에 추가된 `PveBossDungeon` / `ActorMonsterModel_Boss` 딜미터 브로드캐스트 구조 차이**가 핵심으로 보입니다.

우선 조치로는 딜미터 전송을 매 틱/매 데미지성으로 하지 말고 `0.5~1초 단위`로 강제 제한하거나, 상위 N명만 전송하거나, 존 전체 즉시 발송 대신 비동기/큐 기반으로 분리하는 쪽을 추천합니다. 실제 재현 시에는 보스전 시간대의 `[ThreadModel_Zone] Delay Tick` 로그와 `SC_BossDungeonDamageMeter` 발송량을 같이 보면 거의 바로 확인될 가능성이 높습니다.
