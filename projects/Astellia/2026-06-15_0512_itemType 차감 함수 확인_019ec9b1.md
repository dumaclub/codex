# itemType 차감 함수 확인

- Project: Astellia
- Session ID: `019ec9b1-5eec-7f31-9aae-de3ce7885e40`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-06-15T05:11:45.133Z
- Updated: 2026-06-15T05:12:26.7680569Z
- Messages exported: 9

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

itemType 값으로 아이템 리스트를 확인하여 지정된 개수만큼 차감하는 함수가 있는지 확인해 주세요.

### Assistant

코드에서 `itemType`을 기준으로 보유 아이템 목록을 찾고 수량을 차감하는 흐름이 이미 있는지 먼저 훑어보겠습니다. 이름이 조금씩 다를 수 있어서 `itemType`, `ItemType`, 차감 계열 메서드를 같이 좁혀볼게요.

### Assistant

처음 검색은 리소스/캐시 파일까지 빨려 들어가서 너무 넓었습니다. 이제 서버 코드와 실제 C# 소스 쪽으로 범위를 줄여서, 인벤토리/아이템 모델의 메서드 이름과 구현부를 확인하겠습니다.

### Assistant

유력한 후보가 보입니다. `ItemBagModel` 쪽에 `CheckExistItemCount(itemType, count)`와 `RemoveItemByItemType(itemType, count, ...)`가 있고, 실제로 기도/텔레포트 로직에서 호출되고 있습니다. 구현 세부를 열어서 동작이 “리스트 확인 후 지정 개수 차감”인지 확인하겠습니다.

### Assistant

핵심은 `ItemBagModel.RemoveItemByItemType`입니다. 같은 파일 안에서 `RemoveItemByItemTypeOrder`라는 변형도 있어 “정렬 후 차감”까지 지원하는 구조로 보입니다. 이제 정확한 루프와 실패 조건만 잡아오겠습니다.

### Assistant

확인 결과, 수량 체크는 `InventoryModel.CheckExistItemCount(eItemType, long)`이고 실제 차감은 `ItemBagModel.RemoveItemByItemType(eItemType, long, ...)`입니다. 구현상 `_invenItemDic.Values`를 순회해 해당 `ItemType` 수량을 모은 뒤 부족하면 실패하고, 충분하면 앞에서 모은 항목들에서 남은 수량만큼 순차 차감합니다.

### Assistant

이제 호출 예시를 몇 군데 더 확인해서 “사용 가능한 API인지, 아니면 특정 기능 내부 전용인지”를 구분해보겠습니다. 특히 기존 코드가 이 메서드를 어떻게 쓰는지 보면 재사용 위치를 더 정확히 말할 수 있습니다.

### Assistant

한 가지 더 눈에 띄는 건 상위 헬퍼 성격의 `SubCostData`입니다. `eItemType`이 실제 재화면 재화 모델에서 차감하고, 재화가 아니면 인벤토리의 `RemoveItemByItemType`로 넘기기 때문에, “비용 차감” 용도라면 이쪽이 더 의도에 맞을 수 있습니다.

### Assistant

확인했습니다. 이미 있습니다.

핵심 함수는 [ItemBagModel.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Inventory/ItemBagModel.cs:689)의 `RemoveItemByItemType(eItemType itemType, long count, ActorModel actor, eItemRemoveType itemRemoveType, ...)`입니다. `_invenItemDic.Values`에서 `item.GetItemData().ItemType == itemType`인 아이템들을 모아 총 수량을 확인하고, 부족하면 `CannotRemoveRequireItem`을 반환합니다. 충분하면 모은 아이템 리스트를 순회하면서 지정 수량만큼 차감합니다.

별도 보유 체크 함수도 있습니다. [InventoryModel.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Inventory/InventoryModel.cs:539)의 `CheckExistItemCount(eItemType itemType, long checkCount, HashSet<int> passItemId = null)`가 같은 방식으로 `ItemType` 기준 수량을 확인합니다.

이미 사용 중인 예시는 [ActorUserModel_Pray.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Pray.cs:439)에서 체크 후 [468라인](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Pray.cs:468)에 차감하는 흐름이 있고, 공통 비용 처리용으로는 [ActorUserModel_Currency.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Currency.cs:163)의 `CheckCostData`, [206라인](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Currency.cs:206)의 `SubCostData`도 있습니다.

주의할 점은 `RemoveItemByItemType`는 `ItemType`만 보고 차감하므로, 잠금/장착/특정 itemId 제외 같은 정책이 필요하면 호출 전 별도 조건 확인이 필요해 보입니다.
