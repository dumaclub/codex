# Fix 리라 보호막 무한발동 버그

- Project: Astellia
- Session ID: `019d93d5-9ae5-7a51-a1c1-9ae04d53f912`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-04-16T01:09:02.867Z
- Updated: 2026-04-16T01:09:10.7257823Z
- Messages exported: 23

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

Context:
- 개발환경: Windows 11 / Visual Studio 2022
- 라이브 환경 : aws linux 
- Language: C# .net8.0
- Networking: NetMQ
- Architecture: (예: Pub/Sub, Request/Response 등)

Problem:
- 증상:
신화 아스텔 리라 보호막 무한 발동 버그 발생

- 재현 조건:
리라 보호막이 무한으로 재생되고 있습니다. 

- 기대 동작:
한번 사용하면 ReuseCycleTime 40000 값처럼 40초 재사용 시간 이후에 사용이 가능해야 합니다. 
    ReuseCycleTime	int	1	"스킬의 재사용 시간을 설정할 수 있습니다. (1000 = 1초)
    ※ ReuseGroupId에 묶여진 스킬들의 재사용 시간이 남은 스킬을 대상이 스킬 재사용 시간을 만족했을 때, 
    해당 그룹으로 묶여진 스킬들이 사용할 수 있도록 활성화가 되어지는 형태를 가집니다. "	

Code:
리라는 아스텔로 PetData.xlsx에 정보가 있습니다. (아스텔 / 신화 / 황금선율 리라	256002	Pet_Name_256002	황금선율 리라)
리라의 스킬은 SkillBaseData.xlsx 파일에 정보가 있습니다. (아스텔 / 신화 / 황금선율 리라 조건부 발동 스킬A +0 : 본인에게 보호막 생성	25600200)
petSkill과 관련된 코드는 아래 파일에 있습니다. 
ActorModel.cs
ActorModel_Skill.cs
ActorModel_SkillActiveCondition.cs
ActorUserModel_Pet.cs
ActorUserModel_Skill.cs
PetModel.cs
ProjectileActionModel.cs
SkillModel.cs
LoaderPetData.cs
LoaderSkillActiveCondition.cs
LoaderSkillBaseData.cs
LoaderSkillBuff.cs

### Assistant

리라 보호막이 재사용 대기 없이 반복 발동되는 원인을 코드에서 추적해보겠습니다. 먼저 `petSkill` 흐름과 `ReuseCycleTime`/조건부 발동 처리 지점을 같이 찾아서, 실제로 어디서 쿨타임이 빠지는지 확인할게요.

### Assistant

검색 도구가 이 환경에서 막혀 있어서 PowerShell로 바로 추적하겠습니다. 관련 파일과 키워드 위치를 먼저 수집한 뒤, 조건부 발동과 쿨타임 적용 순서를 따라가겠습니다.

### Assistant

관련 키워드 위치는 잡혔고, 이제 실제 서버 로직 파일만 좁혀서 보겠습니다. 조건부 발동 스킬이 "발동은 되는데 쿨타임 등록은 안 되는" 케이스인지 중심으로 확인 중입니다.

### Assistant

대상 서버 파일 경로는 확인됐습니다. 이제 `ActorModel_SkillActiveCondition`에서 조건부 발동 시점, `ActorUserModel_Skill`/`ActorModel_Skill`에서 쿨타임 저장 시점을 이어서 읽어보겠습니다.

### Assistant

조건부 발동 자체의 별도 쿨타임 딕셔너리는 존재하고, 발동 직후 `UpdateCoolTimeForSkillActiveCondition`도 타고 있습니다. 그래서 다음은 이 쿨타임이 중간에 지워지는지, 아니면 펫 버프 재부여 때마다 다시 초기화되는지 확인하겠습니다.

### Assistant

원인 후보가 거의 좁혀졌습니다. `AddSkillActiveCondition()`이 같은 버프가 갱신될 때마다 조건을 다시 등록하면서, 기존 조건부 스킬 쿨타임까지 `TryRemove`로 지우고 있어서 재사용 대기가 계속 초기화될 수 있는 구조입니다. 이제 이 중복 등록을 막는 방향으로 수정하겠습니다.

### Assistant

파일이 UTF-8이 아니라서 패치 도구가 바로 수정하지 못했습니다. 같은 수정 내용을 원본 인코딩을 유지한 채 적용하도록 안전하게 처리하겠습니다.

### Assistant

수정 내용은 확정됐는데, 해당 파일이 읽기 전용이거나 현재 권한 제한에 걸려 있어 직접 저장이 막히고 있습니다. 파일 속성만 확인해서 같은 워크스페이스 안에서 계속 진행하겠습니다.

### Assistant

패치는 반영됐습니다. 이제 변경된 메서드가 의도대로 들어갔는지 확인하고, 이 수정이 다른 흐름을 깨지 않는지 빠르게 검증하겠습니다.

### Assistant

원인 경로도 같이 확인했습니다. 버프 갱신 시 `BuffModel`에서 `AddSkillActiveCondition()`이 다시 호출되는 구조라, 이번 가드가 바로 증상 지점에 닿습니다. 마지막으로 서버 프로젝트가 최소한 컴파일 단계에서 깨지지 않는지만 확인해보겠습니다.

### Assistant

원인은 `AddSkillActiveCondition()`가 같은 버프가 갱신될 때마다 조건부 발동을 다시 등록하고, 그때마다 해당 조건 스킬의 쿨타임까지 지워버리던 구조였습니다. 그래서 리라 보호막 조건부 스킬이 `ReuseCycleTime 40000`을 기다리지 않고 계속 다시 발동할 수 있었습니다.

수정은 [ActorModel_SkillActiveCondition.cs](E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\Actor\ActorModel_SkillActiveCondition.cs:58) 에서 했습니다. 이미 등록된 `PreCheckBuffId`면 `AddSkillActiveCondition()`를 바로 종료하도록 바꿔서, 버프 refresh 시 중복 등록과 쿨타임 초기화가 발생하지 않게 막았습니다. 이 경로는 실제로 [BuffModel.cs](E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\StatusEffect\BuffModel.cs:513) 에서 버프 재적용마다 다시 호출되고 있어서, 이번 수정이 증상 지점에 직접 대응합니다.

검증은 `dotnet build E:\ProjectA02\Trunk\Dev\Server\EgonServer\EgonServer.csproj -c Debug --no-restore`로 시도했는데, 이 환경에서는 `WMIC` 없음과 접근 제한 때문에 빌드가 최종 실패 처리됐습니다. 다만 제 변경으로 인한 C# 컴파일 에러는 출력되지 않았고, 실제 재현 확인은 리라 보호막 발동 후 40초 내 재발동이 막히는지로 보면 됩니다.

### User

재현해 보려면 어떤 스텝으로 진행해야 하나요?

### User

재현해 보려면 어떻게 해야 하나요?

### User

오류 상황을 재현해 보려면 어떤 스텝으로 진행해야 하나요?

### User

업그레이드 해서 지금 plus 모델입니다.

### Assistant

재현 스텝을 정확히 쓰려면 리라 스킬의 조건부 발동 조건과 연결된 버프 ID를 먼저 확인하는 게 안전합니다. 데이터 테이블에서 `25600200`과 관련 `SkillActiveCondition` 행을 찾아서, 실제 QA용 재현 순서를 정리하겠습니다.

### Assistant

조건 테이블까지 찾았습니다. `25600280 -> ApplySkillId 25600200` 연결이 보이므로, 이제 발동 조건과 쿨타임 값을 확인해서 QA 재현 스텝을 구체화하겠습니다.

### Assistant

재현 조건은 거의 정리됐고, 마지막으로 `25600200`이 실제로 어떤 버프를 다시 붙이는지만 확인해보겠습니다. 이게 맞으면 왜 "한 번 터진 뒤 계속 연쇄적으로 보이는지"까지 재현 설명에 같이 넣을 수 있습니다.

### Assistant

재현은 아래 순서로 하시면 됩니다.

1. 테스트 캐릭터에 신화 아스텔 `황금선율 리라`(`PetId 256002`)를 지급하고 소환합니다.
2. 리라의 조건부 발동 패시브가 붙은 상태를 만듭니다. 데이터상 이 패시브는 `PreCheckBuffId 25600280`, 발동 스킬은 `ApplySkillId 25600200`입니다. 조건은 `Defender + OnHit + Self + 5%`라서, 내가 공격하는 게 아니라 “내가 맞는 상황”이 필요합니다.  
[SkillActiveCondition.json](E:\ProjectA02\Trunk\Dev\Client\Assets\0_BalanceData\GameData\SkillActiveCondition.json:1222)
3. 약하지만 타수가 많은 몬스터에게 맞거나, 보조 계정 캐릭터로 계속 때리게 합니다.
4. 보호막이 처음 발동하는 시점을 기록합니다. 실제 보호막 스킬 `25600200`의 `ReuseCycleTime`은 `40000`이라 정상이라면 40초 안에는 다시 발동하면 안 됩니다.  
[SkillBaseData.json](E:\ProjectA02\Trunk\Dev\Client\Assets\0_BalanceData\GameData\SkillBaseData.json:44112)
5. 첫 발동 직후에도 계속 피격을 유지합니다.
6. 40초가 지나기 전에 보호막 이펙트나 버프가 다시 붙으면 버그 재현 성공입니다. 정상이라면 40초 이후에만 재발동해야 합니다.

관찰 포인트는 보호막 자체 버프가 다시 붙는지입니다. 이 스킬은 실제로 `SkillBuff_ID_01 = 25600250` 보호막 버프를 적용합니다.  
[Skillbuffset.json](E:\ProjectA02\Trunk\Dev\Client\Assets\0_BalanceData\GameData\Skillbuffset.json:12201)  
[SkillBuff.json](E:\ProjectA02\Trunk\Dev\Client\Assets\0_BalanceData\GameData\SkillBuff.json:105387)

재현을 쉽게 하려면 “약한 다단히트 적”, “죽지 않을 정도의 생존력”, “첫 보호막 발동 후 계속 맞는 상황” 이 세 가지를 맞추는 게 가장 좋습니다. 5% 확률이라 한두 대로는 잘 안 보일 수 있어서, 빠르게 여러 번 맞는 환경이 좋습니다.

### User

# Files mentioned by the user:

## 녹음 2026-04-16 122952.mp4: E:/Down/녹음 2026-04-16 122952.mp4

## My request for Codex:
기존에 코드로 돌려놓고 재현해 보려고 하는데 정상적으로 잘 되고 있습니다. 16초에 수행 후 56초에 다시 스킬 발동되어 40초 간격으로 진행되고 있습니다. 쿨타임을 보기 위해서 SkillActiveCondition 데이터는 100%로 수정하였습니다.

<image>

[Image attached: base64 payload omitted]

</image>

### Assistant

지금 결과라면 실제 스킬 쿨타임은 정상 적용되고 있다는 뜻이라, 원인을 다시 좁히는 게 맞겠습니다. `SkillActiveCondition`의 1초 쿨과 실제 스킬 `ReuseCycleTime 40000`이 각각 어디서 걸리는지 라인 기준으로 정리해드릴게요.

### Assistant

16초에 1회, 56초에 다시 1회라면 현재 테스트 결과는 “무한 발동”이 아니라 `25600200`의 실제 스킬 쿨타임 40초가 정상 적용된 상태입니다. 이 경우 `SkillActiveCondition`의 `CoolTime = 1`은 조건부 발동 체크 간격일 뿐이고, 실제 재사용 대기는 `SkillBaseData.ReuseCycleTime = 40000`이 막고 있다고 보는 게 맞습니다.  
[SkillActiveCondition.json](E:\ProjectA02\Trunk\Dev\Client\Assets\0_BalanceData\GameData\SkillActiveCondition.json:1222)  
[SkillBaseData.json](E:\ProjectA02\Trunk\Dev\Client\Assets\0_BalanceData\GameData\SkillBaseData.json:44112)

코드 흐름도 그 해석과 맞습니다. 조건부 발동은 [ActorModel_SkillActiveCondition.cs](E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\Actor\ActorModel_SkillActiveCondition.cs:596) 에서 `skillActiveCondition.CoolTime`을 보고 큐에 넣고, 펫이 실제 사용 직전에 [ActorUserModel_Pet.cs](E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\Actor\ActorUserModel_Pet.cs:2794) 에서 `_skillModel.CheckSkill()`로 스킬 쿨을 다시 검사합니다. 실제 40초 쿨 세팅은 [SkillModel.cs](E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\Skill\SkillModel.cs:985) 이후 `ReuseCycleTime`으로 들어갑니다.

그래서 지금 결론은 이렇습니다.
- 현재 로컬 코드와 수정한 데이터(`ActiveRate = 10000`) 기준으로는 “스킬 자체 쿨타임 무시”는 재현되지 않았습니다.
- 즉, 앞서 가정했던 “서버에서 리라 보호막이 실제로 무한 사용된다”는 가설은 이 시나리오만 보면 확정할 수 없습니다.
- 원래 제보가 맞다면 다른 조건이 더 있어야 합니다. 가장 가능성 높은 건 `라이브와 로컬 데이터/코드 차이`, `다른 각성 단계 스킬(25600201~25600205)`, 또는 `실제 재시전이 아니라 클라이언트 이펙트만 반복 재생`입니다.

다음 확인 순서를 권합니다.
1. 보호막 버프 `25600250`가 실제로 40초 전에 다시 “추가”되는지 확인합니다. 이펙트 반복과 실제 재적용을 분리해서 봐야 합니다.  
[Skillbuffset.json](E:\ProjectA02\Trunk\Dev\Client\Assets\0_BalanceData\GameData\Skillbuffset.json:12201)
2. 라이브 데이터에서 `25600280 -> 25600200`, `ReuseCycleTime = 40000` 값이 로컬과 동일한지 비교합니다.
3. 각성 단계별 변형 `25600281~25600285 -> 25600201~25600205`도 같은 방식으로 확인합니다.
4. 서버 로그는 아래 4개만 찍으면 바로 좁혀집니다.
`DoSkillActiveCondition`
`AddSkillInPetSkillQueue`
`_skillModel.CheckSkill`
`AddBuff(25600250)`

현재 결과만 기준으로 보면, 운영 반영 전에 원인 재확인이 먼저입니다. 원하시면 다음 턴에서 “실제 재적용인지 / 이펙트만 반복인지”를 바로 가를 수 있는 로그 포인트 5개만 딱 정리해드리겠습니다.
