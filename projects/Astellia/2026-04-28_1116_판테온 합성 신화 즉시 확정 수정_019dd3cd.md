# 판테온 합성 신화 즉시 확정 수정

- Project: Astellia
- Session ID: `019dd3cd-81e4-7692-9382-8f7826f41fd4`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-04-28T11:15:54.002Z
- Updated: 2026-04-28T11:16:02.6986091Z
- Messages exported: 9

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

[아이템] 아바타 합성 재도전으로 신화 아바타 획득 시 바로 확정되는 현상

[위치]
판테온의 아바타 합성 재도전 쿠폰 (계정) 10031

[내용]
판테온의 아바타 합성 재도전 쿠폰을 통해 신화 아바타 획득 시 신화 아바타가 교체 목록에 노출되지않고 바로 확정되는 현상입니다.

[기대결과]
판테온의 아바타 합성 재도전 쿠폰 사용하여 신화 아바타 획득 시 획득한 신화아바타가 교체 목록에 노출되어야합니다.

QA 팀에서 위와 같은 무의가 와서 확인 중 아래의 [수정 예상 내용]으로 수정하려고 하는데 맞는지 확인해 주세요. 

관련 함수 
static void OnCS_PantheonCoupon_RestoreItem(BasePacket reqPacket, UserGameModel userModel)
public static void OnZonePantheonCouponRestoreItemCmd(BaseZoneCmd baseCmd)
public ePacketCommonResult PantheonCouponRestoreItem(long pantheonCouponItemSerial, int couponType, int seasonNum, long serial)
public ePacketCommonResult RestoreItem(long pantheonCouponItemSerial, PantheonCouponAccountEntity pantheonCouponData, int itemId, int seasonNum, PantheonCouponData pantheonInfo)
public ePacketCommonResult PantheonCouponRestoreAvatar(PantheonCouponAccountEntity pantheonCouponData, int itemId, int failCountRate)

수정 예상 내용 
if (avatarData.AvatarRank >= eItemRank.Epic && avatarData.AvatarRank < eItemRank.Myth)
=> 
if (avatarData.AvatarRank >= eItemRank.Epic && avatarData.AvatarRank <= eItemRank.Myth)

### Assistant

확인해볼게요. 먼저 관련 함수들이 실제로 어떤 흐름으로 이어지는지랑, `Myth` 등급이 다른 합성/재도전 로직에서 어떻게 취급되는지 같이 보겠습니다.

### User

<turn_aborted>
The user interrupted the previous turn on purpose. Any running unified exec processes may still be running in the background. If any tools/commands were aborted, they may have partially executed.
</turn_aborted>

### User

[아이템] 아바타 합성 재도전으로 신화 아바타 획득 시 바로 확정되는 현상

[위치]
판테온의 아바타 합성 재도전 쿠폰 (계정) 10031

[내용]
판테온의 아바타 합성 재도전 쿠폰을 통해 신화 아바타 획득 시 신화 아바타가 교체 목록에 노출되지않고 바로 확정되는 현상입니다.

[기대결과]
판테온의 아바타 합성 재도전 쿠폰 사용하여 신화 아바타 획득 시 획득한 신화아바타가 교체 목록에 노출되어야합니다.

QA 팀에서 위와 같은 무의가 와서 확인 중 아래의 [수정 예상 내용]으로 수정하였는데 맞는지 확인해 주세요. 

관련 함수 
static void OnCS_PantheonCoupon_RestoreItem(BasePacket reqPacket, UserGameModel userModel)
public static void OnZonePantheonCouponRestoreItemCmd(BaseZoneCmd baseCmd)
public ePacketCommonResult PantheonCouponRestoreItem(long pantheonCouponItemSerial, int couponType, int seasonNum, long serial)
public ePacketCommonResult RestoreItem(long pantheonCouponItemSerial, PantheonCouponAccountEntity pantheonCouponData, int itemId, int seasonNum, PantheonCouponData pantheonInfo)
public ePacketCommonResult PantheonCouponRestoreAvatar(PantheonCouponAccountEntity pantheonCouponData, int itemId, int failCountRate)

수정 내용 
if (avatarData.AvatarRank >= eItemRank.Epic && avatarData.AvatarRank < eItemRank.Myth)
=> 
if (avatarData.AvatarRank >= eItemRank.Epic && avatarData.AvatarRank <= eItemRank.Myth)

### Assistant

방금 검색 범위가 너무 넓어서 오래 걸린 것 같습니다. 이번엔 `Pantheon` 관련 파일/심볼만 좁혀서 흐름과 등급 조건의 의미를 확인하겠습니다.

### Assistant

관련 서버 파일 위치는 찾았습니다. 이제 실제 변경 지점 주변과 일반 아바타 합성 보상 처리 쪽을 비교해서 “신화도 교체 목록에 넣는 게 맞는지” 판단해볼게요.

### Assistant

현재 수정된 조건은 서버 코드에 반영되어 있습니다. 다만 같은 “교체 목록” 계열 로직에는 `avatarCnt > 1` 같은 보조 조건도 있어서, 이 변경만으로 부작용이 없는지 더 좁혀 확인 중입니다.

### Assistant

정적 흐름은 거의 결론이 났습니다. 이 분기 하나가 “교체 대기 엔티티 생성 + `changeList` 전송”과 “즉시 카드 지급”을 가르는 지점이라, QA 현상과 직접 맞물려 있습니다.

### Assistant

네, 수정 방향 맞습니다.

기존 조건은 [ActorUserModel_Pantheon.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Pantheon.cs:398)의 `PantheonCouponRestoreAvatar()`에서 보상 아바타를 교체 목록에 넣을지 결정하는 분기입니다.

`eItemRank` 값이 `Epic=4`, `Legendary=5`, `Myth=6`이라서 기존 조건:

```csharp
avatarData.AvatarRank >= eItemRank.Epic && avatarData.AvatarRank < eItemRank.Myth
```

은 영웅/전설만 `AddChangeAvatar()`로 들어가고, 신화는 `else`의 `AddAvatarCard()`로 빠져 바로 확정되는 구조였습니다. 그래서 QA 현상과 원인이 맞습니다.

수정 후 조건:

```csharp
avatarData.AvatarRank >= eItemRank.Epic && avatarData.AvatarRank <= eItemRank.Myth
```

이면 신화도 `AddChangeAvatar()` 처리되고 `SC_AvatarDrawList.changeList`에 실려 클라 교체 목록 갱신 흐름을 타게 됩니다. `Special=99`는 제외되므로 상한을 둔 것도 적절합니다.

참고로 신화 재뽑기/확정 쪽도 `AvatarRedrawData`에 Rank 6 데이터가 있고, 클라 전체 확정 UI도 `Epic`부터 `Myth`까지 순회하므로 흐름상 추가 수정은 없어 보입니다. 빌드는 따로 돌리지 않았고, 코드 흐름 기준 확인입니다.
