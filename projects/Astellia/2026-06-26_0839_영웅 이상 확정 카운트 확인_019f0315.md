# 영웅 이상 확정 카운트 확인

- Project: Astellia
- Session ID: `019f0315-7f99-7aa0-9917-616b5a52b1c2`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-06-26T08:39:28.545Z
- Updated: 2026-06-26T08:39:32.841442Z
- Messages exported: 8

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

저희가 영웅 이상 아바타 / 아스텔의 경우 확정이라는 단계를 거쳐야 하는데, 그 확정을 해야 업적에 횟수가 카운팅 되는걸로 확인되었습니다.
그런데 저희가 아바타 / 아스텔 뽑기를 1회씩아 아니고 한번에 6회 / 11회씩 하는 경우가 있는데 이런 경우에도 영웅 이상이 나오는 경우에 동일하게 확정을 해야
업적이 카운팅 될걸로 보이는데 맞는지 확인 좀 부탁드립니다.

### Assistant

코드 쪽에서 실제 카운팅 지점이 어디인지 확인해볼게요. 우선 아바타/아스텔 뽑기, 확정 처리, 업적 카운트가 연결된 서버 로직을 같이 추적하겠습니다.

### Assistant

검색 결과상 서버에는 확정 처리로 보이는 `AvatarChangeConfirm`, `AvatarChangeAllConfirm`, 그리고 아스텔 쪽으로 보이는 `ConfirmAvatarPetDraw` 흐름이 있습니다. 이제 실제 업적 업데이트가 뽑기 직후인지, 확정 처리 안쪽인지 함수 본문을 열어서 확인하겠습니다.

### Assistant

현재 보이는 구조만 놓고 보면 “확정 패킷” 자체는 방송용 처리에 가깝고, 업적은 카드가 실제로 지급되는 `AddAvatarCard` / `AddPetCard`에서 올라가는 형태입니다. 다만 고등급이 임시 변경 목록에 들어갔다가 확정될 때 지급되는지 확인해야 해서, 그 내부 루프까지 더 들어가겠습니다.

### Assistant

아바타와 아스텔 모두 같은 패턴이 확인됩니다: 영웅(`Epic`) 이상이면 즉시 `Add*Card`로 들어가지 않고 `AddChange*`로 임시 목록에 보관됩니다. 업적 업데이트가 `Add*Card` 내부에 있으므로, 확정 전에는 카운트가 올라가지 않는 구조입니다.

### Assistant

참고로 아바타 쪽 `GetGradedAvatar` 업적은 코드상 주석 처리되어 있고, 현재 직접 올라가는 건 `AvatarAcquisition`입니다. 아스텔은 `PetAcquisition`과 `GetGradedPet` 둘 다 `AddPetCard`에서 올라갑니다.

### Assistant

결론은 거의 잡혔고, 마지막으로 답변에 넣을 정확한 라인만 정리하고 있습니다. 코드 변경은 하지 않고 흐름 확인만 진행 중입니다.

### Assistant

확인 결과, **맞습니다.** 6회 / 11회 뽑기에서도 영웅(`Epic`) 이상 아바타/아스텔이 나오면 즉시 업적 카운트가 올라가는 게 아니라, `changeList`에 들어갔다가 **확정 처리 후** 카드 지급(`AddAvatarCard` / `AddPetCard`)되면서 카운트됩니다.

근거 흐름은 이렇습니다.

- 아바타/아스텔 뽑기에서 영웅 이상이면 `AddChangeAvatar` / `AddChangePet`로 보관하고 `Add*Card`를 호출하지 않음
  [RewardHelper.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Helper/RewardHelper.cs:1202), [RewardHelper.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Helper/RewardHelper.cs:1018)
- 업적 카운트는 실제 카드 지급 함수 안에서 처리됨
  아바타: [ActorUserModel_Avatar.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Avatar.cs:122)
  아스텔: [ActorUserModel_Pet.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Pet.cs:366)
- 확정 시 `AvatarChangeConfirm` / `PetChangeConfirm`에서 `AddAvatarCard` / `AddPetCard`를 호출함
  [ActorUserModel_Avatar.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Avatar.cs:936), [ActorUserModel_Pet.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Pet.cs:988)
- 전체 확정도 내부적으로 각 변경 항목마다 Confirm을 호출합니다
  [ActorUserModel_Avatar.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Avatar.cs:863), [ActorUserModel_Pet.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Pet.cs:844)

따라서 6회 / 11회 뽑기에서 영웅 이상이 여러 개 나오면, 해당 건들은 각각 확정되어야 업적에 반영되는 구조입니다. 단, 낮은 등급처럼 `changeList`에 들어가지 않는 보상은 뽑기 시점에 바로 카드 지급 및 카운트됩니다.
