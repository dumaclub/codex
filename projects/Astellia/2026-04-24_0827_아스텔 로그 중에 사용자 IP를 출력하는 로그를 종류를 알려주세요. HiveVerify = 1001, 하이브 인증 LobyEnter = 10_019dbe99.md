# <environmentcontext>

- Project: Astellia
- Session ID: `019dbe99-95ec-77a1-971e-19ce212b25af`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-04-24T08:27:09.714Z
- Updated: 2026-04-24T08:32:01.869Z
- Messages exported: 9

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

<environment_context>
  <cwd>E:\ProjectA02\Trunk\Dev</cwd>
  <shell>powershell</shell>
  <current_date>2026-04-24</current_date>
  <timezone>Asia/Seoul</timezone>
</environment_context>

### User

아스텔 로그 중에 사용자 IP를 출력하는 로그를 종류를 알려주세요. 
        HiveVerify = 1001, //하이브 인증
        LobyEnter = 1002, //서버 선택(로비 진입)
        CharSelect = 1003, //캐릭터 선택
        GameStart = 1004, //게임서버 진입,
        ZoneLeave = 1005, //존 이탈
        ZoneChange = 1006, //존 이동
        ZoneMove = 1007, //존 내 이동
        Dicconnect = 1008, //접속 종료
        GameEnd = 1009, // 게임 종료
        Tutorial = 1201, //튜토리얼
        ...

### Assistant

요청하신 건 아스텔 로그 타입들 중에서 실제로 사용자 IP를 남기는 종류를 코드 기준으로 확인하는 작업으로 이해했습니다. 먼저 로그 타입 enum 위치와 IP를 기록하는 로깅 경로를 찾아서, 어떤 타입에서 IP가 포함되는지 바로 추려보겠습니다.

### Assistant

`rg` 실행이 이 환경에서 막혀 있어서 PowerShell 검색으로 전환했습니다. enum 정의와 IP 관련 필드를 각각 추적해서 교집합을 찾는 방식으로 확인하겠습니다.

### Assistant

전체 트리를 한 번에 훑으니 바이너리까지 걸려서 잡음이 많았습니다. 서버 소스 파일만 대상으로 좁혀서 enum 정의 파일과 실제 로그 적재 코드를 이어서 보겠습니다.

### Assistant

enum 자체는 `SLogManager`에 있고, 실제 IP 포함 여부는 각 로그 호출부에서 `p3`나 파라미터에 뭘 넘기는지 봐야 확인됩니다. 지금은 접속 계열 로그들의 호출부를 열어서 IP가 들어가는 값을 하나씩 대조하고 있습니다.

### Assistant

호출부 기준으로 보면 접속 계열 로그 대부분은 `p3`에 서버 ID나 캐릭터명만 넣고 있고, IP를 직접 넘기는 건 지금까지 `HiveVerify`만 확인됩니다. 혹시 DB 저장 단계에서 공통으로 IP를 붙이는지 한 번 더 확인해서 누락 없이 정리하겠습니다.

### Assistant

접속 로그 enum 쪽은 거의 정리됐고, 보조로 Hive 분석 로그 쪽에 `client_ip` 필드가 선언만 있는지도 같이 보고 있습니다. 실제 호출이 없다면 “필드는 있지만 현재 enum 로그로는 안 찍힌다”까지 분리해서 말씀드릴게요.

### Assistant

코드 기준으로 보면, 질문하신 `eLogIdx` 접속 계열 로그 중 사용자 IP를 직접 남기는 건 `HiveVerify = 1001` 하나입니다.

- `1001 HiveVerify`
  게이트웨이에서 `GameLogPartition` 호출 시 `p3`에 `packet.resultData.publicIP`를 넣고, 파라미터에도 `publicIP`를 다시 넣습니다. [RepSessionHandler.cs](</E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GatewayNode/PacketHandler/RepSessionHandler.cs:42>)
  이 값은 `GameLogPartition`에서 그대로 `logEntity.p3`와 `parameterString`으로 저장되고, DB `gamelog_partition.p3` 컬럼에 적재됩니다. [SLogManager.cs](</E:/ProjectA02/Trunk/Dev/Server/EgonServer/Managers/SLogManager.cs:577>) [GameLogPartitionMap.cs](</E:/ProjectA02/Trunk/Dev/Server/EgonServer/Database/Mappings/GameLogPartitionMap.cs:11>)

- `1002 LobyEnter`, `1003 CharSelect`, `1004 GameStart`, `1005 ZoneLeave`, `1008 Dicconnect`, `1009 GameEnd`, `1201 Tutorial`
  현재 호출부 기준으로 IP를 넘기지 않습니다. `p3`에는 로그인 ID, 서버 ID, 캐릭터명, 액션 타입 같은 값만 들어갑니다. 예시는 [TaskCmdLayer.cs](</E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GatewayNode/Commands/TaskCmdLayer.cs:534>) 와 [ZoneModel.cs](</E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Zone/ZoneModel.cs:686>) 에서 볼 수 있습니다.

- `1006 ZoneChange`, `1007 ZoneMove`
  현재 코드에서는 실제 호출이 `ZoneChange`로만 남고, `ZoneMove` 호출부는 찾지 못했습니다. [GameUserCmd.cs](</E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Commands/CmdDefines/GameUserCmd.cs:206>)

참고로 Hive 분석용 로그인 스키마에는 `client_ip`/`server_ip` 필드 정의가 있긴 한데, 현재 리포지토리에서 이 경로가 실제로 쓰이는 호출부는 찾지 못했습니다. 그래서 `eLogIdx` 기준으로는 `1001 HiveVerify`만 “사용자 IP가 직접 출력되는 로그”로 보시면 됩니다.

원하시면 바로 이어서 `gamelog_partition` 조회 SQL도 드리겠습니다.
