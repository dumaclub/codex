# nft 기획데이터 시작 설정

- Project: Astellia
- Session ID: `019f3fa2-6172-7da2-a0e0-4a8ac6de9ecb`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-07-08T02:50:36.743Z
- Updated: 2026-07-08T02:50:43.6850971Z
- Messages exported: 10

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

프로세스별 서버 셋팅시
E:\ProjectA02\Trunk\Dev\Server\ServerUp.bat
윈도우에서 위의 파일을 실행시키면 startserver2.ps1 파일을 참조하여 서버가 기동됩니다. 기획데이터를 nft 용으로 시작하게 하고 싶은데 어떻게 수정하면 되나요?

아래는 리눅스 서버에서 nft기획데이터로 서버가 구동되도록 하는 설정입니다.
/usr/local/bin/pm2 start "dotnet EgonServer.dll \"\" \"\" \"\" \"\" \"\" \"\" \"2\" \"2\"" --name ${NAME}

### Assistant

서버 실행 인자 흐름을 먼저 확인해볼게요. `ServerUp.bat`가 `startserver2.ps1`에 무엇을 넘기고, 실제 `dotnet EgonServer.dll` 호출부가 어디서 구성되는지 따라가겠습니다.

### Assistant

`ServerUp.bat`는 단순히 `Server\startserver2.ps1`만 실행하고 있네요. 이제 PowerShell 쪽에서 서버별 실행 인자를 어떻게 만드는지 확인하겠습니다.

### Assistant

핵심은 실행 파일 종류보다 “몇 번째 인자에 `2`가 들어가느냐”입니다. 코드상으로 NFT 데이터 경로를 고르는 값이 `dataLoadType`인 것 같아서, 그 인자의 위치를 정확히 확인하고 있어요.

### Assistant

확인해보니 Windows DEBUG/NFT_SERVER 빌드는 Linux와 인자 배열 구조가 다릅니다. Linux는 `arrParams[6]`이 데이터 로드 타입이고, Windows 실행 분기는 서버명/서버ID가 앞에 붙어서 `winArrParams[7]`이 데이터 로드 타입입니다.

### Assistant

수정 지점은 한 줄입니다. `startserver2.ps1`에서 서버를 띄우는 `Start-Process` 인자 끝에 빈 DB 인자 5개를 채운 뒤, 8번째 Windows 인자인 dataLoadType 자리에 `"2"`를 넣겠습니다.

### Assistant

Git 저장소로는 잡히지 않아서 상태 비교는 못 했지만, 변경은 `Server\startserver2.ps1` 한 줄로만 제한하겠습니다.

### Assistant

이제 PowerShell 문자열이 실제로 원하는 커맨드라인으로 풀리는지 확인하겠습니다. 따옴표 개수가 틀리면 빈 인자가 사라질 수 있어서, 여기만 조심하면 됩니다.

### Assistant

참고로 Linux 예시의 마지막 `"2"` 하나는 이 코드 흐름에서는 `dataLoadType`이고, Windows에서는 그 위치가 한 칸 뒤로 밀립니다. Windows에서 마지막 `"2"`를 하나 더 붙이면 `NewServerGroupId`로 해석되어 그룹 필터까지 걸 수 있으니, NFT 데이터만 목적이면 지금처럼 dataLoadType만 `2`로 주는 쪽이 맞습니다.

### Assistant

수정했습니다: [startserver2.ps1](E:/ProjectA02/Trunk/Dev/Server/startserver2.ps1:24)

현재 실행 인자는 이렇게 풀립니다.

```powershell
/c start EgonServer.exe GlobalGame 2 "" "" "" "" "" "2"
```

Windows 실행 분기에서는 `Program.cs` 기준으로 `winArrParams[7]`이 `dataLoadType`이고, `2`이면 `NodeConfiguration.cs`에서 `../../1_NFTData/GameData/`를 사용합니다. 그래서 `ServerUp.bat`는 그대로 두고 `startserver2.ps1`의 `Start-Process` 한 줄만 수정하면 됩니다.

구문 파싱은 확인했고, 실제 서버 기동은 하지 않았습니다.
