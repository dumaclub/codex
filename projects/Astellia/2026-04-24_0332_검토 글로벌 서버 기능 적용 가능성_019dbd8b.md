# 검토 글로벌 서버 기능 적용 가능성

- Project: Astellia
- Session ID: `019dbd8b-8d70-7773-a21d-f1ac95c43cdf`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-04-24T03:32:12.824Z
- Updated: 2026-04-24T03:32:26.4019022Z
- Messages exported: 2

> Export note: Codex ?? ???/??? ???, ?? ?? ??, base64 ??? ??? ???? ???/assistant ??? ??????.

## Conversation

### User

기획이나 사업의 특정 기능 개발에 대해서 이 기능이 구현 가능할 것 같은데로 생각하며 개발하니 상충되어 오류 발생하는 구문이 정말 많은 것 같습니다. 
예를 들어 글로벌 게임서버, 인터 게임서버, 게임서버, ... 다 같은 코드를 적용한 게임 서버인데 글로벌 게임서버에서만 특정 기능을 하기 위해 작업을 하고 
게임서버나 인터서버에서는 예외처리 되도록 개발하는데 기획내용이 또 바뀌고 추가되면서 이 기능을 현재 코드에 넣어도 되는지 아닌지를 판단하기가 너무 힘듬니다. 
어떻게 확인해야 할까요?

### Assistant

이 경우는 보통 코드 실수보다, `공통 기능`과 `서버별 예외`를 구분하는 기준이 코드 밖에 없어서 생기는 문제에 가깝습니다.  
지금처럼 `글로벌만 다르게`, `인터는 예외`, `기획 변경으로 다시 수정`이 반복되면, 코드에 `if (Global)`, `if (!Inter)` 같은 분기가 계속 쌓이면서 판단이 더 어려워집니다.

**확인 기준**
기능 추가 전에 먼저 이것부터 문서로 고정해야 합니다.

1. 이 기능은 `모든 서버 공통 규칙`인가
2. 아니면 `특정 서버에서만 켜지는 옵션`인가
3. 아니면 `서버별로 로직 자체가 다른 기능`인가
4. 이 예외가 `임시`인지 `상시 정책`인지
5. 기획 변경 시 어떤 서버까지 같이 수정되어야 하는지

이 5개가 정리되지 않으면, 코드를 어디에 넣어도 나중에 충돌할 가능성이 큽니다.

**실무적으로는 이렇게 보시면 됩니다**
- 공통 규칙이면: 공통 코드에 넣습니다.
- 서버별 `사용 여부`만 다르면: 코드 분기보다 `설정값/Feature Flag`로 뺍니다.
- 서버별 `행동 방식`이 다르면: 공통 코드에 억지로 넣지 말고 `Policy/Strategy`처럼 서버별 구현을 분리합니다.
- 임시 예외면: 코드에 넣더라도 `언제 제거할지`, `누가 판단할지`를 같이 남겨야 합니다.

예를 들면 `if (IsGlobalServer)`를 곳곳에 넣는 대신,
`ServerCapability.EnableX`,
`MatchPolicy`,
`RewardPolicy`
처럼 “서버 종류”가 아니라 “기능 정책” 기준으로 묻게 바꾸는 편이 훨씬 안전합니다.

**가장 효과 큰 방법 3개**
- 기능별 적용 범위 표를 만드세요.  
  `기능명 / 글로벌 / 인터 / 일반 게임서버 / 공통코드 여부 / 예외 처리 / 테스트 케이스`
- 개발 전에 짧은 변경 체크리스트를 두세요.  
  `적용 서버`, `예외 서버`, `설정으로 뺄 수 있는지`, `프로토콜/DB 영향`, `테스트 대상`
- 코드 리뷰 때 “구현 가능성”보다 먼저 “이 기능이 어느 계층 책임인지”를 확인하세요.  
  공통 계층인지, 서버 정책 계층인지, 운영 설정인지부터 정해야 합니다.

핵심은 `코드에서 판단하지 말고, 문서와 구조에서 먼저 판단 가능하게 만드는 것`입니다.  
원하시면 제가 다음 답변에서 바로 쓸 수 있는 `기능 적용 범위 체크리스트`나 `서버별 정책 분리 구조 예시`를 실무형으로 정리해드릴게요.
