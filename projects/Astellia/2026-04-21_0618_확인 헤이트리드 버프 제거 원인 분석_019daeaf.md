# 확인 헤이트리드 버프 제거 원인 분석

- Project: Astellia
- Session ID: `019daeaf-e29e-7051-8177-26456adcd357`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-04-21T06:17:35.681Z
- Updated: 2026-04-21T06:18:23.8015203Z
- Messages exported: 9

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

버프 제거 오류 확인 
현상 : 헤이트리드는 15초간 유지되는 디버프 인데 브루탈 스윙을 시전할 경우 유지시간을 다 채우지 못하고 헤이트 리드가 제거됩니다. 15초가 유지 못하는 이유는 무었인가요?   SkillBaseData.xlsx 
10200747  브루탈 스윙 +7 특성 B : 주변 추가 대미지
10200847  헤이트리드 +7 특성 B : 대미지 내성 감소 추가

<image>

[Image attached: base64 payload omitted]

</image>

### Assistant

확인해볼게요. 먼저 `SkillBaseData.xlsx`와 관련 스킬/버프 테이블 구조를 찾아서 10200747, 10200847이 어떤 버프/그룹/제거 조건으로 연결되는지 추적하겠습니다.

### Assistant

`rg` 실행 권한이 막혀 있어서 PowerShell 파일 검색으로 넘어갑니다. 엑셀은 읽기만 하면서, 두 스킬 ID가 참조하는 버프 ID와 제거/중복 그룹 설정을 따라가겠습니다.

### Assistant

두 위치(`Table`, `Table_Balance`) 모두 같은 이름의 테이블이 있어서 우선 둘 다 확인합니다. 이제 엑셀 내부에서 해당 ID가 실제로 어떤 행과 컬럼에 걸려 있는지 뽑아볼게요.

### Assistant

`Skillbuffset` 쪽에서 단서가 보입니다. 헤이트리드는 1020084702와 1020084704 버프를 걸고, 브루탈 스윙 특성 B는 별도 세트 ID `7500`을 참조하고 있어서 이제 그 버프 정의와 중복 타입을 확인하겠습니다.

### Assistant

방금 출력에서 `7500/8500`은 버프 ID가 아니라 확률값이었습니다. 실제 브루탈 스윙 버프는 `1020074701`, 헤이트리드는 `1020084701/4702/4704`라서 이 네 버프의 그룹/중첩 방식 중심으로 다시 좁혀봅니다.

### Assistant

핵심 원인은 거의 잡혔습니다. 브루탈 스윙의 `제압` 버프와 헤이트리드의 메인 `증오` 디버프가 같은 `skillBuffGroupID=1020090001`에 들어 있고 둘 다 “덮기 가능”이라, 코드상 같은 그룹 버프 적용 시 교체되는 흐름인지 확인만 더 해보겠습니다.

### Assistant

서버 처리도 확인했습니다. 같은 그룹의 기존 버프가 있고 새 버프 레벨이 같거나 높으면 기존 버프 ID를 제거 큐에 넣는 코드가 있어서, 데이터 설정만으로도 재현되는 케이스입니다.

### Assistant

확인 결과, **헤이트리드가 15초를 못 채우는 이유는 지속시간 설정 문제가 아니라 `skillBuffGroupID` 중복으로 인한 덮어쓰기 제거**입니다.

`Table/SkillBuff.xlsx` 기준으로 보면:

| 스킬 | 적용 버프 | 타입 | 그룹 | 중첩 | 지속 |
|---|---:|---|---:|---|---:|
| 브루탈 스윙 +7 특성 B | `1020074701` | 제압 | `1020090001` | 덮기 가능 | 2초 |
| 헤이트리드 +7 특성 B | `1020084701` | 증오 | `1020090001` | 덮기 가능 | 15초 |

두 버프가 **같은 그룹 `1020090001`**이고, 둘 다 **`OverlappedType=1`, 즉 덮기 가능**입니다. 서버 처리에서도 같은 `skillBuffGroupID`의 버프가 이미 있으면 그룹 목록을 가져온 뒤, 새 버프 레벨이 기존보다 낮지 않으면 기존 버프를 제거 큐에 넣습니다. 관련 흐름은 [BuffModel.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/StatusEffect/BuffModel.cs:369) 쪽입니다.

즉 흐름은 이렇습니다.

1. 헤이트리드가 대상에게 `1020084701`을 적용함
2. `1020084701`은 15초 지속이지만 그룹이 `1020090001`
3. 브루탈 스윙이 `1020074701`을 적용함
4. `1020074701`도 같은 그룹 `1020090001`, 같은 레벨 `4`, 덮기 가능
5. 서버가 헤이트리드 `1020084701`을 제거하고 브루탈 스윙 `1020074701`로 교체
6. 그래서 헤이트리드가 15초 전에 사라짐

참고로 `SkillBaseData.xlsx`의 `SkillRemoveID`는 두 스킬 모두 `0`이라서, **스킬 제거 리스트로 지우는 문제가 아닙니다.** 원인은 `SkillBuff.xlsx`의 버프 그룹 충돌입니다.

수정 방향은 의도에 따라 둘 중 하나입니다.

- 헤이트리드와 브루탈 스윙이 동시에 유지되어야 한다면, 브루탈 스윙 `1020074701` 또는 헤이트리드 `1020084701`의 `skillBuffGroupID`를 서로 다른 그룹으로 분리하는 것이 가장 안전합니다.
- 헤이트리드가 같은 그룹 상태이상에 의해 교체되면 안 된다면, 헤이트리드 `1020084701`의 `OverlappedType`을 “덮어쓰기 불가능”으로 바꾸는 방향도 가능하지만, 같은 그룹의 다른 상태이상 적용까지 막을 수 있어 영향 범위 확인이 필요합니다.
