# 로컬 기획데이터 경로 확인

- Project: Astellia
- Session ID: `019f5980-8def-74f1-a24b-32ee41dc77d0`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-07-13T03:23:50.036Z
- Updated: 2026-07-13T03:24:36.5751792Z
- Messages exported: 18

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

로컬 서버에서 서버 실행시 기획데이터를 로컬에서 수정한 데이터로 가져오는 것인지 확인할 수 있는 로그가 있나요? 기획데이터 폴더를 출력하는 로그나 기획데이터 파일 경로를 알고 싶습니다.

### Assistant

코드에서 기획데이터 로딩 지점과 로그 키워드를 먼저 찾아볼게요. 로컬 서버 실행 경로가 서버 쪽 설정/모델 초기화에 숨어 있을 가능성이 있어서, 관련 문자열부터 넓게 훑겠습니다.

### Assistant

첫 검색은 클라이언트 데이터 파일까지 잡혀서 너무 넓었습니다. 서버 쪽으로 범위를 줄이고, `Table` 폴더와 실제 로딩 클래스명을 기준으로 다시 좁혀볼게요.

### Assistant

서버 쪽에는 `ServerDataStream`이 데이터 루트 경로를 들고 있고, 배치 파일들이 실행 인자를 넘기는 구조로 보입니다. 이제 `ServerDataStream` 참조와 로컬 서버 시작 스크립트가 어떤 경로/모드를 지정하는지 확인해볼게요.

### Assistant

기본 경로는 실행 폴더 기준 상대경로로 잡혀 있고, 실제 로컬/QA/NFT 선택은 `NodeConfiguration`에서 하는 듯합니다. 그 파일과 서버 시작 인자 해석 부분을 직접 보겠습니다.

### Assistant

이미 존재하는 확인 로그가 하나 보입니다: `NodeConfiguration Init` 직후에 DEBUG 빌드에서 `gameDataPath : ...`를 찍습니다. 다만 각 파일명까지 찍는지는 `DataManager` 로더 쪽을 더 봐야 정확합니다.

### Assistant

현재 실행 설정 파일에는 `RES_PATH`도 별도로 있습니다. 이 값이 `GameData/`와 합쳐져 JSON 기획데이터 경로가 되고, Debug에서는 그 최종값이 로그로 남습니다.

### Assistant

`Server\SharedLib\DataManager` 폴더가 비어 있거나 생성물 제외 상태라, 로더 구현은 클라이언트 쪽 공유 코드에 있는 것 같습니다. 서버 프로젝트가 참조하는 파일 위치를 확인해서 파일 단위 로깅 여부까지 마저 보겠습니다.

### Assistant

파일 단위 로딩은 클라이언트 `Assets\Script\SharedLib\DataManager`의 공유 코드가 서버에서도 쓰이는 구조입니다. 이제 해당 로더에서 성공/실패 로그가 어떤 형태인지 확인하겠습니다.

### Assistant

로더 내부는 성공 시 파일 경로를 매번 찍지는 않고, 파일이 없거나 비어 있을 때만 `filepath`를 포함한 에러 로그를 남깁니다. 로그 파일 위치를 알려드리려면 `SLogManager`가 어디에 쓰는지까지 확인해볼게요.

### Assistant

검색 결과가 너무 커서 로그 매니저 파일만 이름으로 좁히겠습니다. 필요한 건 로그 디렉터리/파일명 규칙뿐이라 그 부분만 보겠습니다.

### Assistant

로그는 기본적으로 Windows에서 `C:\AstelM\Log`로 쓰도록 되어 있습니다. 마지막으로 실제 파일명 패턴만 확인하면, 바로 grep할 키워드까지 정리해서 드릴 수 있습니다.

### Assistant

실제 로그 파일은 `Total-날짜.log` 형태로 쌓이고 있네요. 최근 로그에 `gameDataPath`가 남아 있는지도 한 번만 확인해서, 바로 찾을 수 있는 검색어를 확정하겠습니다.

### Assistant

최근 로그에도 해당 줄이 찍혀 있습니다. 출력 폭 때문에 일부가 잘렸으니, 가장 최신 로그의 해당 라인만 원문으로 뽑아보고 마무리하겠습니다.

### Assistant

정리하면 서버는 `Table/*.xlsx`를 직접 읽는 구조가 아니라, 생성된 `Client\Assets\...\GameData\*.json` 쪽을 읽습니다. 답변에 로컬 기준 실제 해석 경로와 확인 로그 키워드를 같이 적겠습니다.

### Assistant

로컬 실행 배치별 인자도 함께 보면 오해가 줄어듭니다. `dataLoadType` 값 0/1/2가 실제 어떤 데이터 폴더를 선택하는지, 배치 파일 이름과 같이 확인하겠습니다.

### Assistant

한 가지 미묘한 점이 있습니다. `gameDataPath` 로그는 코드상 `DEBUG` 조건부 블록 안에 있어서, 로컬 Debug 실행에서는 확실히 보이고 Release 계열에서는 안 보일 수 있습니다. NFT_SERVER 구성의 define도 확인해두겠습니다.

### Assistant

네. 로컬 서버 실행 시 확인할 수 있는 로그가 있습니다.

로그 파일 위치는 기본적으로 `C:\AstelM\Log\Total-*.log` 입니다. 여기서 아래 키워드로 찾으면 됩니다.

```powershell
Select-String -Path C:\AstelM\Log\Total-*.log -Pattern "gameDataPath|LoadConfig\(\) => RES_PATH" |
  Select-Object -Last 20
```

현재 코드 기준 주요 로그는:

- `LoadConfig() => RES_PATH : ...`
  config의 리소스 루트 출력: [ConfigLoader.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/Util/ConfigLoader.cs:74)

- `gameDataPath : ..., nftDataPath : ..., {dataLoadType}`
  실제 서버가 읽을 GameData 경로 출력: [NodeConfiguration.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/NodeConfiguration.cs:47)

최근 로그 예시는 이렇게 찍혀 있었습니다.

```text
LoadConfig() => RES_PATH : ../../../../../Client/Assets/ResourceBundles/Data/
gameDataPath : ../../../../../Client/Assets/ResourceBundles/Data/../../1_NFTData/GameData/, nftDataPath : ../../../../../Client/Assets/ResourceBundles/Data/../../1_NFTData/, 2
```

해석하면 `dataLoadType = 2`라서 NFT 데이터, 즉 로컬 기준 대략 이 경로를 읽습니다.

```text
E:\ProjectA02\Trunk\Dev\Client\Assets\1_NFTData\GameData\
```

배치별로는:

- `LocalServerStart.bat`: `dataLoadType = 0`, 일반 데이터
  `E:\ProjectA02\Trunk\Dev\Client\Assets\ResourceBundles\Data\GameData\`

- `LocalServerStart_UpdateQA.bat`: `dataLoadType = 1`, 밸런스/업데이트 데이터
  `E:\ProjectA02\Trunk\Dev\Client\Assets\0_BalanceData\GameData\`

- `LocalServerStart_NFT.bat`: `dataLoadType = 2`, NFT 데이터
  `E:\ProjectA02\Trunk\Dev\Client\Assets\1_NFTData\GameData\`

주의할 점은 서버가 `Table/*.xlsx`를 직접 읽는 게 아니라, 생성된 JSON을 읽는 구조입니다. 파일별 실제 읽기 경로는 `stream.JSONPathRoot() + filename`으로 만들어집니다: [BaseLoaderFileReader.cs](E:/ProjectA02/Trunk/Dev/Client/Assets/Script/SharedLib/DataManager/BaseLoaderFileReader.cs:73). 파일이 없으면 전체 경로가 에러 로그로 찍힙니다: [ServerDataStream.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerDataStream.cs:37).
