# 리턴유저 판정 기준 확인 করুন

- Project: Astellia
- Session ID: `019d99e8-f167-7bb0-a3a7-58b631f0c883`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-04-17T05:27:53.492Z
- Updated: 2026-04-17T05:28:01.8192061Z
- Messages exported: 7

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

public bool CheckReturnUser(DateTime loginDate) 리턴유저인지 체크하는 함수인데 몇일동안 미접속해야 리턴유저로 판단되는건가요?

### Assistant

`CheckReturnUser(DateTime loginDate)`가 실제로 어떤 기준으로 계산하는지 코드에서 바로 확인해볼게요. 우선 함수 정의와 호출부를 찾아서 리턴 유저 판정 기준 일수를 확인하겠습니다.

### Assistant

`rg` 실행이 막혀 있어서 PowerShell 검색으로 바로 전환했습니다. 함수 정의가 있는 파일과 관련 상수를 같이 찾아서 기준 일수를 좁혀보겠습니다.

### Assistant

관련 로직은 찾았습니다. 지금은 실제 판정 기준이 코드에 하드코딩인지, 아니면 상수 테이블에서 읽는 값인지 확인해서 정확한 일수를 말씀드리겠습니다.

### Assistant

판정 흐름상 `ReturnUser`와 `ReturnUser_Period` 두 값이 나뉘어 있습니다. 하나는 “복귀 유저로 인정되기 위한 미접속 일수”, 다른 하나는 “복귀 유저 상태 유지 기간”이라서 둘 중 어떤 값인지 함수 본문까지 확인하고 정리하겠습니다.

### Assistant

기준값까지 확인됐습니다. 이제 함수명 기준이 아니라 실제 밸런스 데이터 값 기준으로 정리해서 말씀드릴게요.

### Assistant

복귀 유저 판정 기준은 `7일`입니다.

코드상으로는 `GetCheckReturnUserDay()`가 상수 `"ReturnUser"` 값을 읽고 있고, 그 값이 `7`로 설정돼 있습니다.
- [LoaderConstantConfig.cs](E:/ProjectA02/Trunk/Dev/Client/Assets/Script/SharedLib/DataManager/Loader/LoaderConstantConfig.cs:309)
- [ConstantConfig.json](E:/ProjectA02/Trunk/Dev/Client/Assets/0_BalanceData/GameData/ConstantConfig.json:1059)

추가로 헷갈리기 쉬운 값이 하나 더 있는데, `"ReturnUser_Period"`는 `30일`입니다. 이건 “몇 일 미접속하면 복귀 유저가 되느냐”가 아니라, 복귀 유저로 판정된 뒤 그 상태를 `30일간 유지`하는 값입니다.
- [AccountInfo.cs](E:/ProjectA02/Trunk/Dev/Client/Assets/Script/AccountInfo.cs:116)
- [ConstantConfig.json](E:/ProjectA02/Trunk/Dev/Client/Assets/0_BalanceData/GameData/ConstantConfig.json:1411)

즉 정리하면:
- 미접속 `7일` 이상: 복귀 유저 판정
- 복귀 유저 상태 유지: `30일`

원하시면 `CheckReturnUser(DateTime loginDate)` 실제 본문도 같이 찾아서 이 값이 어떻게 비교되는지 더 이어서 확인해드릴게요.
