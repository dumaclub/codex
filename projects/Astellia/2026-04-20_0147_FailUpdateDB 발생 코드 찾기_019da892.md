# FailUpdateDB 발생 코드 찾기

- Project: Astellia
- Session ID: `019da892-4484-7b11-aabd-e103c40b09ab`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-04-20T01:47:31.379Z
- Updated: 2026-04-20T01:47:38.7300713Z
- Messages exported: 7

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

길드 가입 요청 -> 암호 입력 시 (FailUpdateDB = -21,) 이 에러가 발생되었는데 어느 코드에서 발생되었는지 확인해 주세요. 
CS_GuildJoin = 423, 길드 가입 패킷 
static void OnCS_GuildJoin(BasePacket reqPacket, UserGameModel userModel)
public void GuildJoin(eGuildJoinType joinType, long guildId, int password, string introduce)
static void OnCS_ZMQ_GuildJoin(BasePacket reqPacket)
public static void OnGuildJoinReqCmd(BaseGuildCommunityCmd baseCmd)
...

### Assistant

확인해볼게요. 먼저 패킷/길드 가입 흐름과 `FailUpdateDB` 정의를 코드 전체에서 좁혀서, 에러 코드 `-21`이 어디서 만들어지거나 전달되는지 추적하겠습니다.

### Assistant

`rg`가 실행 권한 문제로 막혀 있어서 PowerShell 기본 검색으로 우회하겠습니다. 같은 키워드를 파일 범위별로 훑고, 결과가 많으면 가입 처리 경로 쪽만 더 파고들게요.

### Assistant

검색이 너무 넓어서 클라이언트 바이너리/패키지까지 걸렸습니다. 이제 서버 쪽 `GameNode -> CommunityNode -> DBNode` 흐름만 좁혀서 실제 `-21` 생성 지점을 보겠습니다.

### Assistant

현재 흐름상 후보는 세 군데입니다: GameNode에서 클라 패킷을 ZMQ로 넘기는 부분, CommunityNode에서 가입 요청을 검증/DB 요청하는 부분, DBNode에서 실제 길드 멤버 저장을 수행하는 부분입니다. 각 파일의 주변 코드를 줄 번호 포함으로 열어보겠습니다.

### Assistant

비밀번호 가입은 `NeedAccept`가 아니라 `JoinDirect` 저장 타입으로 떨어집니다. 이제 `JoinDirect`가 DB 저장에서 실패하면 `FailUpdateDB`로 바뀌는 정확한 지점을 확인하고 있습니다.

### Assistant

확인 결과, `FailUpdateDB = -21`은 길드 가입 로직의 검증 단계에서 직접 만든 에러가 아니라 **DBNode에서 길드 정보 저장 실패 시 세팅되어 돌아오는 결과값**입니다.

가장 직접적인 발생 지점은 여기입니다.

- [DBHelper.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/DBNode/Helper/DBHelper.cs:674)  
  `DBHelper.UpdateGuildSync()` 안에서:
  ```csharp
  result = communityDBController.UpdateForGuild(guildInfo, recruitList)
      ? ePacketCommonResult.Success
      : ePacketCommonResult.FailUpdateDB;
  ```

즉, 암호 입력 후 가입 요청 흐름에서 `UpdateForGuild()`가 `false`를 반환하면 `FailUpdateDB(-21)`로 변환됩니다.

흐름은 이렇게 이어집니다.

1. 클라 `CS_GuildJoin = 423` 수신  
   [ThrowedHandler.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/PacketHandler/ThrowedHandler.cs:4025)  
   `OnCS_GuildJoin()` → `actorUserModel.GuildJoin(...)`

2. GameNode에서 CommunityNode로 가입 요청 전달  
   [ActorUserModel_Guild.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/GameNode/Models/Actor/ActorUserModel_Guild.cs:392)  
   `GuildJoin()` → `CS_ZMQ_GuildJoin` 생성, `Password` 파라미터 포함

3. CommunityNode에서 요청 처리  
   [CommunityHandler.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/CommunityNode/PacketHandler/CommunityHandler.cs:632)  
   `OnCS_ZMQ_GuildJoin()` → `GuildJoinReqCmd`

4. 실제 가입 검증 및 저장 요청  
   [GuildCommunityManager.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/CommunityNode/Managers/GuildCommunityManager.cs:362)  
   `ReqGuildJoin()`

   여기서 비밀번호가 틀리면 [GuildCommunityManager.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/CommunityNode/Managers/GuildCommunityManager.cs:562) 의 `WrongGuildJoinPassword`로 빠집니다.  
   이번처럼 `FailUpdateDB`가 왔다면 비밀번호 검증은 통과했고, 이후 `JoinDirect` 저장 단계로 넘어간 상태입니다.

5. Password 가입은 `JoinDirect`로 DB 저장 요청  
   [GuildCommunityManager.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/CommunityNode/Managers/GuildCommunityManager.cs:424)  
   `joinType == AutoJoin || Password` → `GuildMemberEntity` 생성 → `SendZMQGuildInfoSyncSave(eGuildInfoSyncSaveType.JoinDirect, ...)`

6. DBNode에서 저장 수행  
   [DBHandler.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/DBNode/PacketHandler/DBHandler.cs:1464)  
   `OnCS_ZMQ_GuildInfoSyncSave()` → `DBHelper.UpdateGuildSync(...)`

7. DB 저장 실패가 `FailUpdateDB`로 변환  
   [DBHelper.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/DBNode/Helper/DBHelper.cs:645)  
   `UpdateGuildSync()` → [DBHelper.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/ServerNodes/DBNode/Helper/DBHelper.cs:674) 에서 `FailUpdateDB`

실제 DB 저장 함수는 여기입니다.

- [DatabaseController.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/Controllers/DatabaseController.cs:2092)  
  `UpdateForGuild()`

- [DatabaseController.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/Controllers/DatabaseController.cs:2137)  
  `UpdateGuildDataByDB()`  
  여기서 `guild`, `memberList`, `relationList`, `recruitList` 등을 DB에 업데이트하고 `Commit()`합니다. 예외가 나면 [DatabaseController.cs](E:/ProjectA02/Trunk/Dev/Server/EgonServer/Controllers/DatabaseController.cs:2190) 의 catch에서 `false`를 반환합니다.

따라서 결론은:

**이번 `FailUpdateDB(-21)`는 `OnCS_GuildJoin` 자체가 아니라, Password 가입 처리 후 `GuildMemberEntity`를 DB에 저장하는 `CS_ZMQ_GuildInfoSyncSave -> DBHelper.UpdateGuildSync -> DatabaseController.UpdateForGuild` 경로에서 발생한 것입니다.**

추가로 원인 확인은 DBNode 로그에서 아래 문자열을 보면 됩니다.

- `query error =>`
- `OnCS_ZMQ_GuildInfoSyncSave`
- `UpdateForGuild`
- `GuildMemberEntity`
- 가입 대상 `charId`, `guildId`

가능성이 큰 실제 원인은 `guild_member` insert/update 중 DB 예외입니다. 예를 들면 이미 같은 `charId`의 길드 멤버 row가 남아있거나, FK/중복키/트랜잭션 commit 예외가 난 경우입니다.
