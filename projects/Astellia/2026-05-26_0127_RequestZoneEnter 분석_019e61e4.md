# RequestZoneEnter 분석

- Project: Astellia
- Session ID: `019e61e4-9f5a-79f0-8f25-855325ddf01d`
- Workspace: `E:\ProjectA02\Trunk\Dev`
- Started: 2026-05-26T01:27:13.499Z
- Updated: 2026-05-26T01:27:20.1912715Z
- Messages exported: 8

> Export note: internal system/developer instructions, tool execution logs, and base64 image payloads are omitted. Only user/assistant messages are archived.

## Conversation

### User

<environment_context>
  <cwd>E:\ProjectA02\Trunk\Dev</cwd>
  <shell>powershell</shell>
  <current_date>2026-05-26</current_date>
  <timezone>Asia/Seoul</timezone>
</environment_context>

### User

존 입장 함수 분석해 주세요. 
public bool RequestZoneEnter(eZoneChangeType zoneChangeType, UserGameModel userModel, long channelKey)

### Assistant

함수 선언만으로는 흐름을 짐작하기보다 실제 구현과 호출 지점을 같이 보는 게 정확하겠네요. 먼저 `RequestZoneEnter` 정의와 관련 타입/호출부를 찾아서 입장 플로우를 따라가 보겠습니다.

### Assistant

전체 검색이 SQL 덤프까지 훑으면서 너무 커졌습니다. 서버 쪽 C# 파일로 범위를 좁혀서 함수 본문과 `ZoneEnterCmd` 주변부터 보겠습니다.

### Assistant

정의 위치는 `ZoneManager.cs`입니다. 이제 본문, 이 함수를 호출하는 커맨드, 그리고 `eZoneChangeType` 값을 같이 읽어서 “입장 요청이 어떤 입력을 받아 어떤 Zone/Channel로 보내는지” 중심으로 정리해볼게요.

### Assistant

본문상으로는 단순 입장보다 “대상 존 산정 -> 기존 채널 예약 -> 없으면 존 생성 -> 유저 바인딩”까지 맡는 함수입니다. 예약 유저 수가 입장 가능 판단에 들어가는지, 실패 시 어떤 정리가 되는지도 확인하겠습니다.

### Assistant

한 가지 눈에 띄는 지점이 있습니다. `channelKey` 파라미터를 받지만 검증 조건이 요청값이 아니라 `nextLocation.zoneChannelKey`를 보고 있어서, 의도와 다를 가능성이 있어 보입니다. 클라이언트가 어떤 `zoneChangeType`으로 보내는지도 확인해서 이게 실제 위험인지 보겠습니다.

### Assistant

**핵심 요약**

[RequestZoneEnter](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Managers\ZoneManager.cs:623>)는 “존 입장 완료” 함수라기보다, **입장할 ZoneModel을 찾거나 만들고, 유저를 예약 등록한 뒤 UserGameModel을 해당 존에 바인딩하는 함수**입니다. 바인딩 후 실제 캐릭터 데이터 로딩과 스폰은 후속 흐름에서 진행됩니다.

**호출 흐름**

클라이언트 `CS_ZoneEnter` 수신  
→ [OnCS_ZoneEnter](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\PacketHandler\ThrowedHandler.cs:43>)  
→ [ZoneEnterCmd.RunProc](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Commands\CmdDefines\GameUserCmd.cs:351>)  
→ `ZoneManager.Instance.RequestZoneEnter(...)`

`ZoneEnterCmd`에서는 먼저 유저 상태가 `Active`인지, 기존 `ActorModel`이 남아 있지 않은지 검사합니다. 실패하면 킥 또는 `FailedZoneEnter` 처리로 빠집니다.

**RequestZoneEnter 내부 흐름**

1. 이전 존 저장  
`userModel.PrevZoneId = currentLocation.zoneId`

2. 입장 대상 `zoneId` 결정  
[UserGameModel.GetChangeZoneId](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\User\UserGameModel.cs:247>) 기준입니다.

`LastPosition`이면 캐릭터의 `lastZoneId`를 사용하고, 없으면 `CharacterStartZone`으로 보냅니다.  
`ZoneMove`이면 `transferModel.nextLocation.zoneId`를 사용합니다.  
그 외 `Resurrection`, `ChannelMove` 등은 여기서는 직접 지원하지 않고 `-1`이 됩니다.

3. 존 생성 가능 여부 확인  
[CanCreateZone](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Managers\ZoneManager.cs:1325>)에서 존 데이터 존재, 콘텐츠 블록 여부, HiddenBoss 조건, 현재 서버에서 생성 가능한 존인지 확인합니다.

4. 채널키 결정  
기본값은 `userModel.transferModel.nextLocation.zoneChannelKey`입니다. `channelKey` 파라미터가 있어도 무조건 쓰지 않고, Normal 존 채널 조건을 통과할 때만 덮어씁니다. 이후 `WorldDungeonManager.Instance.GetZoneChanelKey(...)`가 월드 던전용 채널키를 다시 보정할 수 있습니다.

5. ZoneType별 분기  
`GuildPlace`, `GuildDungeon`, `UiDungeon`, `DominationBattle`, `PveBossDungeon`, `HiddenBoss`는 `_zoneChannelKeyLinkMap` 기반으로 연결된 채널을 찾거나 새 존을 생성합니다.

`TeamFightSingle`, `TeamFightGuild`는 지정 채널에서 입장 가능한 존을 찾습니다.

그 외 일반 존은 기존 입장 가능 존을 찾고, 없으면 새 `ZoneModel`을 생성합니다. `Normal` 타입은 채널 분산용 스레드를 쓰고, 그 외는 가중치가 낮은 존 스레드를 선택합니다.

6. 예약 입장 등록  
기존 존을 찾으면 `zoneModel.AddReservedEnterUser(...)`로 예약 유저에 추가합니다. 이 예약 수는 [GetUserCount](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\Zone\ZoneModel.cs:537>)에 포함되어, [Enterable](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\Zone\ZoneModel.cs:1852>)의 최대 유저 수 판단에도 반영됩니다.

7. 바인딩  
[BindZone](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\User\UserGameModel.cs:306>)이 성공하면 `currentLocation`을 새 존으로 갱신하고 DB에 `CS_ZMQ_QueryActorData`를 보냅니다. 이후 DB 응답에서 [CompleteZoneEnter](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Models\User\UserGameModel.cs:490>)가 `ActorUserModel`을 만들고 `SC_ZMQ_ZoneEnter`를 전송합니다.

**리턴값 의미**

`true`는 “클라이언트가 완전히 스폰됨”이 아니라, **존 예약 및 바인딩이 성공했고 DB 캐릭터 데이터 조회가 시작됨**을 뜻합니다.

`false`면 [ZoneEnterCmd](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Commands\CmdDefines\GameUserCmd.cs:371>)가 `FailedZoneEnter`를 보내고, 현재 위치 또는 마지막 위치 기준으로 `RebindGameNode`를 시도합니다.

**주의할 점**

`CS_ZoneEnter.zoneId`는 클라이언트 패킷에 있지만 “삭제 예정” 주석이 있고 서버 입장 결정에는 사실상 쓰이지 않습니다. 서버는 `zoneChangeType`과 `userModel.transferModel` 상태를 신뢰합니다.

`channelKey` 파라미터 처리도 조금 헷갈립니다. [651라인](<E:\ProjectA02\Trunk\Dev\Server\EgonServer\ServerNodes\GameNode\Managers\ZoneManager.cs:651>)에서 요청받은 `channelKey`가 아니라 기존 `zoneChannelKey`를 검사한 뒤 요청값으로 덮어씁니다. 의도적으로 `nextLocation` 중심으로 설계한 흐름일 수 있지만, “요청 channelKey 자체를 검증한다”는 의미는 아닙니다.

또 `AddReservedEnterUser(...)`의 실패 리턴을 호출부가 확인하지 않습니다. 중복 입장 요청 같은 비정상 상황에서 예약 실패 후에도 `BindZone`으로 진행될 여지가 있습니다.
